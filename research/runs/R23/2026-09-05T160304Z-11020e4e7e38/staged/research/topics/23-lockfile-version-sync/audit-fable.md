decision-sha256: ea269dd6ac3342672f48e69e6c2cb2645c7f73d1e476e5306a20803d1a6598ed
actor: audit-fable-2026-09-05T160304Z-11020e4e7e38
model: claude-fable-5-1
family: anthropic
verdict: approve
unresolved-findings: none

# R23 lockfile-version-sync — judgment audit (2026-09-05)

Judgment audit of `review/DECISION.md` (revision 1, 2026-09-05) for item R23 (`lockfile-version-sync`, kind `pattern`, Light tier, owns no parameter), run `research/runs/R23/2026-09-05T160304Z-11020e4e7e38`. The auditor is a fresh context that produced none of the run's inputs. Read in full: `inputs/prompt.md` (sha256 `96c95ab5…`, byte-identical to `research/topics/23-lockfile-version-sync/prompts/lockfile-version-sync.prompt.md` and to the manifest's `input_hashes.prompt`), `raw/codex.md` (sha256 `28cdcb73…`, `scripts/check-answer-shape.sh … pattern` passes), `raw/evidence-terra.md` (sha256 `9ff54558…`, evidence-check result sound, figures ok 0 / wrong 0 / unverifiable 0), `review/DECISION.md`, `review/audit-codex.md` (arrived during this audit; see Findings), `review/empirical/run-checks.sh`, `review/empirical/rp-driver.cjs`, the three fixtures, `review/evidence/cargo-lock-sync-acceptance.log`, `docs/port/PARAMETERS.md`, spec §2, `docs/port/BASELINE-REVIEW.md:69,143,250,312`, `docs/port/COMMONALITY.md:69-85,139`, `docs/port/DIVERGENCE-ANALYSIS.md:107,263`, `docs/port/OWNER-REVIEW.md:65`, `research/RUNBOOK.md` and `docs/planning/p02/acceptance-schema.md`. Light tier: one research engine plus the fresh Terra evidence check is the complete required input (`research/RUNBOOK.md` §2 table; `inputs/EXECUTION.json` R23 `engines: ["codex"]`, `evidence_checks: ["terra"]`); the absence of an Opus or Doxa report is not a gap.

## Principle and agreement level

The decision states the shared principle as: a release PR is one complete version transaction, so every committed file that encodes the package's own version — manifest, release-please version record, and the lockfile when the lockfile records the root package's version — changes in the same commit, with no follow-up sync commit. It records the agreement level as an architectural pattern with a policy-level acceptance rule (completeness of the release commit is shared; updater syntax is not). That is the right level under spec §2's table: the source repos share the completeness rule and differ only in lockfile schema, which `docs/port/DIVERGENCE-ANALYSIS.md:107` classes as cause C (language-bound mechanic, `harmonize: no`) and `docs/port/OWNER-REVIEW.md:65` accepts as written (disposition `accept`, run in full).

Source evidence was verified against both repos at their pinned revisions, not from memory:

- py `release-please-config.json:16-20` at `b08bccf` (read with `git show`): an `extra-files` entry `{"type": "toml", "path": "uv.lock", "jsonpath": "$.package[?(@.source && @.source.editable && @.source.editable.value=='.')].version"}` — matches the decision's quotation. py `tests/meta/test_version_consistency.py:56-71` at `b08bccf` holds `test_manifest_matches_pyproject` and `test_uv_lock_matches_pyproject` (the lock comparison ends at `:71`), matching `docs/port/COMMONALITY.md:139`.
- ts `.github/workflows/release-please.yml:27-30` at `cb1cbcb`: "pnpm-lock.yaml does not encode the root project's own version. The generated release commit is therefore complete as-is: do not add a post-creation lock synchronization commit, which would expose an inconsistent intermediate PR revision to CI and automated reviewers." — matches the decision's quotation. ts `release-please-config.json` at `cb1cbcb` has no lockfile `extra-files` entry.

The decision correctly reads Rust as py-shaped on the principle (fixture case S1 records `name = "demo-single"` / `version = "0.1.0"` in `Cargo.lock`; the Cargo workspaces reference places one lockfile at the workspace root) and as needing a different updater only because `release-type: rust` already schedules a `Cargo.lock` writer. What must agree (release PR complete as generated; every root-version copy in it; CI proves consistency) and what may vary (lockfile membership; the updater) are stated explicitly, and the six essential behaviors are each tied to an executed case. The principle is preserved rather than weakened: the decision requires the lock change in the same update set (S4) and adds an enforcement half (`--locked`, S2/S3) that makes the completeness rule observable in CI.

## Design and evidence

Architecture alternatives were compared before any mechanism was chosen, and the selection follows from the principle and executed evidence rather than from popularity or repository agreement:

- (A) native Rust strategy — one writer per file, package-aware matching; executed in S4 (`Rust.buildUpdates scheduled 2 update(s): Cargo.toml, Cargo.lock`, one-line lock diff, `cargo build --locked` 0, three meta-tests pass). Selected.
- (B) generic TOML `extra-files` jsonpath (py's shape) — executed in S5a/S5b: the natural path `$.package[?(@.name=="demo-single")].version` produces `⚠ No entries modified`, exit 0, lock unchanged, `--locked` still 101; the tagged path `@.name.value` works and is byte-identical to (A). Runner-up, with its silent-failure mode documented. The decision's explanation (`GenericToml` parses through `TaggedTOMLParser`, `util/toml-edit.js:63`) matches the installed source.
- (C) post-PR `cargo update -w` commit — S9 shows byte identity with (A)'s output, so it buys nothing and violates the completeness rule ts states. Excluded.
- (D) do not commit `Cargo.lock` — excluded on the application-artifact argument (CLI and web service) and Cargo's FAQ. Justified.
- (E) release-plz / cargo-release — out of scope because the release manager is settled (F063, F066, F073 `COMMON → REUSE`). Correct scoping.

Library and version claims were checked against primary sources on this host: `review/empirical/node_modules/release-please/package.json` is `17.11.2`, license Apache-2.0, `engines.node >= 22.0.0`; `review/empirical/package-lock.json` pins `release-please@17.11.2` from `registry.npmjs.org`; `https://raw.githubusercontent.com/googleapis/release-please-action/v5.0.0/package.json` declares `"release-please": "^17.6.0"` (fetched during this audit); `docs/port/COMMONALITY.md:69` (F063) records `release-please-action v5.0.0, checked 2026-09-02`. Every code line the decision quotes was found where cited: `strategies/rust.js:108-112` pushes `Cargo.lock` with `createIfMissing: false` and `new cargo_lock_1.CargoLock(versionsMap)`; `updaters/rust/cargo-lock.js:41-59` matches `[[package]]` entries by name and replaces only `version`; `updaters/rust/cargo-toml.js:42-62` walks the versions map over dependency kinds and bumps path dependencies that carry a `version`; `plugins/cargo-workspace.js:220-224` pushes a root `Cargo.lock` update with `CargoLock(updatedVersions)`; `github.js:617-621` calls `mergeUpdates` on duplicate paths, and its `try/catch` (`:625-636`) wraps only `getFileContentsOnBranch`, so `update.updater.updateContent(...)` at `:640` throws through — the decision's statement that a real run aborts on the S6a/S6c throw is correct.

The empirical check demonstrates the principle rather than startup. `review/evidence/cargo-lock-sync-acceptance.log` has sha256 `5ab26e3782ad9d32dd35ed17e0800f8c0670275e4839420c0544097faeee7050` (700 lines) as the decision records, with `summary: PASS=77 FAIL=0 RECORD=5` and `RESULT: PASS`; 77 `PASS:` lines were counted. Each acceptance criterion in the decision's table maps to an observed case with matching output: S2 (stale lock refused by `cargo build --locked` and `cargo metadata --locked`, exit 101, lock byte-identical to pristine), S3 (plain `cargo test` rewrote the lock before the assertion ran, `1 passed`; the `.release-please-manifest.json` assertion fails at the same state with `left: "0.1.0" right: "0.2.0"`; `cargo test --locked` exits 101 with zero `running` lines), S4, S5a/S5b, S6a/S6c (virtual root: `is not a package manifest (might be a cargo workspace)`; globs not expanded), S6d/S6e (per-crate packages plus plugin lock step; dependency requirement bumped `from 0.1.0 to 0.2.0`), S7/S7c (inherited bump invalidates `demo-lib = "^0.1.0"`; plugin `ConfigurationError … invalid [package.version]`), S8 (1.96.0 builds `--locked`; 1.91.1 refused with `requires rustc 1.96`) and S9. The driver's simulation boundary (filesystem stub of the two GitHub calls those classes make; `.release-please-manifest.json` edited by the driver; plugin `run()` not executed) is stated honestly and matches `rp-driver.cjs`.

Performance is correctly declared not a selector (one in-memory TOML edit per release). The reference-implementation gap (no named third-party repository verified to release with `release-type: rust` and a committed `Cargo.lock`) is stated, not hidden, and release-please's own `test/strategies/rust.ts` is cited as the executable reference.

## Completeness

Required decision fields (`research/RUNBOOK.md` §5; `docs/planning/p02/acceptance-schema.md`): `## Decision` carries the recommendation, the version (`release-please-action` v5.0.0 / release-please `^17.6.0`, 17.11.2 executed), the settled row F065 (`docs/port/COMMONALITY.md:71`), a nonempty `### Principles and implementation`, and the line `re-verify: 2027-03-01, or earlier on …` outside any fence; `## Parameters`, `## Empirical check` (toolchain, OS, command, working directory, observed output) and `## Engines` (both reports, five disagreements each with the settling evidence, and stated remaining uncertainty) are present; no `## Supersedes` (not a reversal). Each H2 and the H3 appear once.

Parameters: owned none (the prompt's `- owns:` is empty) and consumed none (`- consumes:` empty; `inputs/prerequisites.json` is `{}`), both stated. The four `- assumes` lines match `docs/port/PARAMETERS.md` byte for byte: `msrv-policy`, `rust-edition` = `2024`, `target-os-matrix` = `ubuntu-latest, macos-latest`, `license` = `MIT OR Apache-2.0`. No `CONFLICT:` line was needed and none was raised by either the raw report or the decision.

Fitness gates 1-6 are each answered with `inapplicable` plus the reason where a crate metric does not describe a pattern, as the prompt's tier guidance requires; gate 2 is additionally exercised on the cargo side at the policy floor (S8); gate 4 states the action runs on GitHub's runner, macOS was executed and Linux is planned. Every citation in the raw report and the decision carries a retrieval date of 2026-09-05. All four prompt questions are answered from executed evidence: HIGH-1 (lock encodes the member version and goes stale) by S1/S2/S3/S9; HIGH-2 (jsonpath vs native updater vs companion step) by S4/S5a/S5b; MEDIUM (pre-tag CI step) by S9 plus the completeness rule; LOW (multi-crate) by S6/S7 and the topology table for R02. The prompt's value test resolves consistently: `release-please-config.json` gains `release-type: rust`, gains no `Cargo.lock` `extra-files` object, and the release-PR diff includes the `Cargo.lock` version line.

`BASELINE-REVIEW:` handling: the raw report emitted `BASELINE-REVIEW: F133 …` (as its receipt records) and the decision carries its own `BASELINE-REVIEW: F133` line with feature ID, affected items (R41 for wiring, R22 for the accessor) and evidence (S3). The decision did not edit `docs/port/COMMONALITY.md` or `docs/port/BASELINE-REVIEW.md`; it reports the finding for controller adjudication with F133's owners as `research/RUNBOOK.md` §4 requires, and it is consistent with the reconciliation already recorded at `docs/port/BASELINE-REVIEW.md:312` (`COMMON → REUSE` kept, guard scope clarified, accessor assertion retained and left to R22). The synthesizer is a distinct actor (`synth-fable-…`) from the raw producer (`research-codex-…`) and the evidence checker (`evidence-terra-…`), as the schema requires.

## Findings

Findings requiring a change to the decision before acceptance: none.

Consistency of the empirical audit: `review/audit-codex.md` was absent when this audit began and arrived while it was in progress (sha256 `09284e5697d507796e156fa689f658868605f62abeae050e09447677654c5aa6` as read). It binds to the same decision hash `ea269dd6…`, records actor `audit-codex-2026-09-05T160304Z-11020e4e7e38`, model `gpt-5.6-terra`, family openai (a different actor and model family from this audit, as the schema requires), approves with no unresolved findings, and records a rerun of `bash run-checks.sh` on a fresh copy of `review/empirical` with exit 0, `summary: PASS=77 FAIL=0 RECORD=5`, `RESULT: PASS`, release-please 17.11.2 and the same toolchain — the rerun log `review/evidence/audit-lockfile-version-sync.log` was inspected directly and shows those lines. Its findings (S2 discriminating, S4 reproduced, S5a/S5b, six primary sources spot-checked including the `^17.6.0` range) agree with this audit's independent checks. The two audits are consistent with each other and with the decision.

Observations already disclosed in the decision as residual uncertainty (not blocking):

1. Linux was not executed; only macOS 26.4 arm64 was. The recommendation contains nothing OS-sensitive: `--locked` is documented Cargo behavior independent of platform, and the release-please action runs on GitHub's runner regardless of the `target-os-matrix`. The decision labels the `ubuntu-latest` run as planned.
2. The release-please version embedded in `release-please-action` v5.0.0's `dist/index.js` was not diffed against the executed 17.11.2. The raw report inspected the bundle (read, not executed) and found the Rust strategy scheduling `CargoLock`; the decision sets a re-verify trigger on a release-please change to `strategies/rust.js` or `updaters/rust/cargo-lock.js`.
3. No named third-party Rust repository releasing with `release-type: rust` and a committed `Cargo.lock` was verified by any party; the decision states this gap and relies on release-please's own strategy tests as the executable reference.

Controller follow-ups (not defects in R23):

4. Attribution of the `--locked` wiring. The decision assigns "how and where that guard is wired — hook, CI step or job" to R41. R41's prompt (`research/topics/41-lockfile-freshness-check/…`) scopes a pre-commit hook (ledger row F171); neither the R11 (`ci-job-structure`) nor the R32 prompt names `--locked` for CI cargo invocations, and `docs/planning/p02/dependency-reconciliation.md:35` records no descriptive coupling for R23. Essential behavior 4 and criterion S2 depend on `--locked` being applied to the CI cargo invocations somewhere. The decision states this as an assumption, which `research/RUNBOOK.md` §1 and §5 make the controller's check; the controller should attach the requirement as an acceptance-criteria note to whichever item wires the CI cargo commands, not only to R41.
5. Scope of the S3 wording in the `BASELINE-REVIEW: F133` line. S3 proves that a test reading the working-tree `Cargo.lock` cannot observe manifest-to-lock drift under cargo (cargo rewrites the lock before the test binary runs, or `--locked` fails before any test runs). A test that read the committed lock (for example via `git show HEAD:Cargo.lock`) would be observable, so "cannot observe the drift" is slightly broader than the evidence. This does not change the decision's conclusion that `--locked` is the effective guard; it is offered for the controller's adjudication of F133.

## Assessment

Approve. The decision states the shared principle at the correct agreement level with pinned evidence from both source repositories, preserves it with a native design chosen by comparing five architectures against executed evidence rather than by popularity or repository agreement, and answers every prompt question, fitness gate and required field with dated sources. The empirical check (77 asserted cases, 0 failures) demonstrates the principle from both sides — the stale lock is refused and the native updater changes the lock in the same update set — and its simulation boundary is stated honestly. The `BASELINE-REVIEW: F133` finding is reported with evidence and left to the controller as the runbook requires; no `CONFLICT:` arises. The independent empirical audit reproduces the run and approves on the same decision hash. Remaining uncertainties are disclosed, bounded, and carry re-verify triggers; none requires the decision to change.
