decision-sha256: 20693ce171bcde1e4e0bbe1c8b03e3b11a5619d2824ed6578af8abf6305791ba
actor: audit-fable-2026-09-05T160304Z-99a3dd6456c0
model: claude-fable-5-1
family: anthropic
verdict: approve
unresolved-findings: none

## Principle and agreement level

The decision states the shared requirement as one contributor-recognition implementation: the local recipe and the CI bot run the same `contributors-please` engine with the same rendering rules over one committed configuration (`.contributors.yml`) and one committed ledger (`.contributors.jsonl`), and the projection (`CONTRIBUTORS.md`) is reproducible from those files without network access. It sets the agreement level at **architectural pattern** and sources it correctly: `docs/port/DIVERGENCE-ANALYSIS.md:175` (R47 row, class A, `harmonize: yes`), `docs/port/OWNER-REVIEW.md:88` (`accept`), and D-024(9) at `ts-launch-blueprint/docs/port/TS_PORT_DECISIONS.md:329,342` ("single implementation for recipe and workflow"; adopted to eliminate "the documented recipe/script divergence"). I re-read all three at the cited lines in the local pinned clones; they say what the decision says.

The level is right under spec §2. What must agree is a set of responsibilities and relationships (project from committed state; discover only to bootstrap; read the same three files the action reads), and what may vary is mechanism (runner spelling, Justfile syntax, provisioning). The tool itself is not selected at this level: it is inherited from F060 and F209 (`COMMON → REUSE`, classed policy/mechanism at `docs/port/BASELINE-REVIEW.md:52,101`), which the decision consumes unchanged. That layering is correct: R47 owns the recipe's lifecycle shape, not the tool.

The principle is not silently weakened; it is sharpened. The prompt's context paraphrases D-024(9) as "ts's `render` reproduces the same ledger-to-Markdown step the CI action performs". The decision refines this to: `render` is the action's projection stage, while the action's `pull-request` mode first runs discovery, identity join, classification and merge, then commits, pushes, labels and opens the PR. I verified that reading against the local clones: `contributors-please/src/engine/contributors.ts:129-145` (`run()`: shallow check, state-file requirement unless `bootstrap`), `:278-281` (`openPullRequest()` calls `run()` first), `src/cli.ts:53-78` (`render` reads config, state file, and the output file when `in_place`, then writes), `contributors-please-action/src/index.ts:531-551` (`pull-request` dispatch). The design that preserves the principle (guarded bootstrap plus render) is native and justified rather than inherited: neither precedent practices the guard, the decision says so, and the justification is the fresh-template constraint (empty history, no committed ledger) plus the engine's own error text, which prescribes exactly this split (`contributors.ts:141`, verified).

## Design and evidence

Architecture alternatives were compared before any tool question, each with a concrete cost. (A) py's unconditional `init` performs remote discovery and rewrites the ledger on every local run. (B) ts's unconditional `render` fails on ts's own pinned checkout: `git ls-tree -r --name-only cb1cbcb2e88b898e8c081b0abbfabc1630079c00` in `~/c/ts-launch-blueprint` lists `.contributors.yml`, `.github/workflows/update-contributors.yml` and `CONTRIBUTORS.md` and no `.contributors.jsonl`; case 08 reproduces the resulting `ENOENT`. (C) the guard is selected. (D) a Rust-native tool is excluded for the absence of any candidate, with the absence claim bounded (one crates.io search, HTTP 403 on the evidence check's recheck) and stated as such. (E) `git-cliff` is excluded by capability with the missing capability named. No crate figures were invented; the decision writes `inapplicable` with the reason, as the prompt's tier guidance requires. Selection follows from the principle and the source behavior, not from popularity or repository agreement.

The "same engine" link is stronger than the decision states. The action imports `Contributors` from `"contributors-please"` (`~/c/contributors-please-action/src/index.ts:12,126`), its `package.json` depends on `file:../contributors-please`, and its lockfile pins `../contributors-please` at `1.4.3` both at `main` HEAD (`cbb1fd47…`) and at the SHA ts's CI actually pins, `3c6dbff9ef21190aa1f63b3c400ed4f0ab404ac1` = tag `v1.3.9` (`package-lock.json:29-30` at that SHA). The release running in both sources' CI therefore embeds the exact version the fixture pins.

The empirical check demonstrates the principle, not startup. Eleven cases and 51 assertions cover the guard in both directions (02 and 03: `render` with zero requests to the API stand-in and an unchanged ledger sha; 04: bootstrap with exactly one `GET /api/v3/repos/smorinlabs/rs-launch-blueprint/contributors?per_page=100`; 05: the flip back to `render` with the request count unchanged), determinism (03 and 05 byte-identical), five inverse controls (06 missing owner, 07 API 500, 08 direct `render` without a ledger, 09 missing marked output, 10 shallow clone; each exits 1, writes no ledger, leaves the output untouched) and runner independence (11: `bun x`, byte-identical, clean `git status`). "No discovery" is measured by a hermetic stand-in for the single endpoint `init` calls (`review/empirical/fake-github.mjs`), so it is observed rather than assumed. Case 02b compares the recipe's bytes with a direct engine `render`, which ties the recipe to the engine rather than to its own output.

Non-obvious choices are evidenced. I reproduced the `@`-prefixed shebang behavior on `just 1.57.0`: `@quiet:` prints its two-line script body before running it, the un-prefixed recipe is silent, so dropping `@` is correct. The `${GITHUB_SERVER_URL:+…}` passthrough mirrors the action's own resolution chain (`index.ts:99-102`, verified) and is exercised both unset (`run-checks.sh:20` unsets it; cases 02, 03, 09, 11) and set (cases 04-07, 10). The two "explained from source" observations are correct: `src/engine/github.ts:111-112` sets `name: row.login`; `src/engine/config.ts:98` defaults `multiCategoryResolution` to `"priority"`; `src/engine/classifiers/path.ts:50-63` takes the first configured category in config order.

The audit-codex rerun is consistent with the decision: same decision hash, a fresh copy of the fixture with an isolated npm cache, exit 0, `summary: PASS=51 FAIL=0`, per-case output identical to the synthesizer's log. Its five citation spot checks returned 200 with matching content; the two ts raw URLs returned 404, and the local pinned clone verifies both (`Justfile:243-244`; workflow `:60` `ubuntu-latest`, `:74` action, `:81` `mode: pull-request`, `:85` `bootstrap: 'true'`).

## Completeness

- H2 structure: exactly `## Decision`, `## Parameters`, `## Empirical check`, `## Engines`, in order, no `## Supersedes`; `### Principles and implementation` present; `re-verify:` is the final line of `## Decision` (line 95) with a date and the events that re-open it.
- Settled row: F210 (`docs/port/COMMONALITY.md:214`, verified); F060 and F209 consumed as they stand.
- Parameters: the prompt's `- owns:` line is empty and the decision says so; four `- assumes` lines quote `docs/port/PARAMETERS.md:7-10` character for character; the prompt's `- consumes:` line is empty and no `CONFLICT:` line appears in the raw, the evidence check or the decision, which is correct.
- Fitness gates: gate 1 passes (MIT; confirmed by the evidence check and by `~/c/contributors-please/package.json:5`); gates 2, 3, 5 and 6 are `inapplicable` with the reason (never a Cargo dependency), exactly as the prompt requires for a pattern item; gate 4 is stated honestly (macOS 26.4 arm64 executed; Linux inferred from both sources' `ubuntu-latest` workflows running the same engine under `node24`, `ts …update-contributors.yml:60` and `py …update-contributors.yml:18`, both verified). Maintenance state `active` with dates. Every source date is 2026-09-05.
- Empirical section: toolchain, OS, command, working directory, observed output and log path present; the fixture is retained under `review/empirical/` and its runner is bounded (40 x 0.25 s readiness poll, kill on exit).
- Engines: actors, models and families match `review/identities.json` and `manifest.json` (Codex `gpt-5.6-luna` via `codex-cli-0.153.2`; Terra evidence check `gpt-5.6-terra`). I re-ran `scripts/check-answer-shape.sh raw/codex.md pattern` (`OK: validation passed`). I recounted the evidence check from `raw/evidence-terra.md`: 5 + 4 + 4 = 13 ok, 0 wrong, 2 unverifiable, as the decision reports. Light tier: one engine plus one fresh evidence check is the complete required input; the absence of an Opus or Doxa report is not a gap.
- `BASELINE-REVIEW:` and `CONFLICT:` handling: correct; see Findings item 1.

## Findings

Unresolved: none.

Observations for the controller (none changes the decision):

1. `BASELINE-REVIEW: F210 …` disposition. Codex applied the tag to this item's own `DIVERGENT` row, not to a `REUSE` or `ADOPT` baseline, and its "proposed change" is R47's own recommendation. The synthesizer recorded the line transparently (Engines table row 2) as a refinement of the D-024(9) rationale, changed no ledger row, and left F060 and F209 unchallenged. I concur with that classification. RUNBOOK §4 makes the controller the adjudicator, so record this disposition at publication rather than forwarding it as a baseline change.
2. Runner-ownership edge. `docs/port/BASELINE-REVIEW.md:101` (F209) hands the Node-runtime invocation question to "F210 under R47". R47 answers the runtime question (no Rust-native equivalent; the recipe shells out to Node `>=24` either way) and defers the runner spelling and pin (`npx` / `pnpm dlx` / `bun x`; `@1` versus exact) to R42's `package-manager-invocation`. R42's prompt names neither `contributors-please` nor a Node package runner, and `docs/port/PARAMETERS.md:14` does not list R47 among that parameter's consumers. Not decision-changing: case 11 proves the recipe body is runner-agnostic, and the prompt's LOW question framed exactly this deferral. Dependency-reconciliation note: give the runner spelling an owner (a note in R42's prompt or an R47 to R42 consumer edge) so it is not orphaned between the two items.
3. Stated, unexecuted checks: Linux locally (gate 4 inferred from CI), a live bootstrap against GitHub, the action-parity byte comparison (needs a live repository and the App secrets), and the `pnpm dlx` spelling. Each is named as planned in the decision, and the source-level evidence for action parity (engine import, lockfile at the pinned release) is strong. Acceptable for Light.
4. Empirical-auditor fixture path. `docs/superpowers/plans/p02-roles/empirical-auditor.md` names "a fresh fixture under `review/empirical-audit/`"; the rerun used `/private/tmp/R47-empirical-audit.sj6svc`, a fresh copy of `review/empirical/`, and saved the complete log at `review/evidence/audit-terra.log` as required. The acceptance schema records any `cwd`. Procedural only.
5. Version alignment. The action reports `1.3.9` while embedding `contributors-please` `1.4.3`; the decision already lists this relationship as a re-verify trigger, and my lockfile check at `v1.3.9` confirms the embedded version, so no caveat applies today.

## Assessment

I approve revision 1 of the R47 decision. It states the shared principle at the architectural-pattern level with the correct sources, preserves it with a native design whose justification (empty-history template; `render` has no bootstrap path; the engine's own error text prescribes the split) is verified against the package and action source, and compares the real alternatives on their costs rather than on popularity or on which source repository happened to do what. Every required field, the four fixed parameters, the applicable and inapplicable gates, and the source dates are present and correct. The empirical check exercises the guard in both directions, measures the absence of discovery, and includes five inverse controls; the independent rerun reproduces it exactly. The `BASELINE-REVIEW:` line was classified correctly and no `CONFLICT:` was owed. The observations above are for the controller's reconciliation and publication records, not for the decision.
