decision-sha256: 2fc432d4384bf492c03b7276fab4458c8cc52d734ef73e60f68d1ebc378e9c99
actor: audit-fable-2026-09-05T151356Z-c46d732f98ed-r3
model: claude-fable-5-1
family: anthropic
verdict: approve
unresolved-findings: none

## Principle and agreement level

Judged on the declared two-engine scope: `## Engines` states that the Doxa report is absent (blocked on billing and auth, `operations/doxa.json`) and that this revision will be superseded when it arrives. This audit was produced by a fresh context that wrote none of `raw/codex.md`, `raw/opus.md`, `review/DECISION.md` (revision 3, sha256 `2fc432d4…`) or `review/audit-codex.md`, and that did not write the earlier audits `review/audit-codex.r1.md`, `review/audit-codex.r2.md` or `review/audit-fable.r2.md`.

The decision states the shared principle at the right level and preserves it. The principle is "every commit that lands satisfies one declared Conventional-Commits contract: grammar and the eleven-type vocabulary for every commit without exception; width rules for every human-authored commit, relaxed only in CI and only for bot pull requests whose generated messages cannot be wrapped; rejected before creation by the `commit-msg` hook and checked again in CI so `git commit --no-verify` cannot evade it". The level is capability/standard, and both citations hold at the stated line numbers: `docs/port/BASELINE-REVIEW.md:75` records F148 ("Enforce the selected commit-message convention at `commit-msg` time", capability/standard, retained), and `docs/port/DIVERGENCE-ANALYSIS.md:155` classes the R38 tool row as class B with `harmonize: partly`, while the value rows F152-F155 (`:157-160`) are class A with `harmonize: yes`. F149 is `COMMON → SUBSTITUTE` with `parent: F148` (`docs/port/COMMONALITY.md:153`), so replacing commitlint with `committed` is a tool substitution under an unchanged pattern row; no `OVERRIDE` is due and none is claimed. Both raw reports read the level the same way, and the decision quotes each.

The shared low-level values (the eleven types, the 50-column subject cap, the 72-column body and footer caps) sit at spec §2's policy/mechanism level inside that capability, and spec §2 requires a fit argument at that level rather than an inference from repository agreement. The decision supplies it: Git's own `git-commit` DISCUSSION and Pro Git for 50/72; ts already enforcing 50/72; py's only recorded reason for 200 (permalinks) dissolved in py's own ecosystem because the installed `@commitlint/cli@21.0.2` exempts any line matching `\bhttps?://\S+` (`review/evidence/commitlint-21.0.2-comparison.log:3`, cases 05 and 07 accepted, case 08 rejected at a 72-column footer rule, so the exemption covers the footer rule too); and the one residual class that would still tighten py (a long non-URL token, comparison case 17) named and left to py's follow-on evaluation. That is the honest shape of the evidence. The revision-3 addition to the owned value, the bot width-relaxation clause, is stated inside the parameter rather than left implicit, which keeps the principle concrete instead of weakening it into a vague goal.

## Design and evidence

**Architecture before libraries.** Three alternatives are compared as owner amendment A5 requires: (A) provision a JS runtime and run commitlint, (B) a native binary with its own configuration file, (C) a hand-rolled regex. The comparison is genuine: (A) is the choice of both source repositories and of the Codex report, and the decision departs from it on stated, checkable costs (`engines.node >= 22.12.0`, a `package.json` and JS lockfile in a Rust repository, py's three measured silent-pass modes). Repository agreement was treated as evidence, not as the winner.

**Library selection follows capability and gates, not popularity.** `convco check` enforces no width rule; `cocogitto`'s `Settings` has no length field and is `deny_unknown_fields`; `git-sumi` counts whole lines and declares no `rust-version` (gate 2 unmet); `commitlint-rs` ships no release binaries. `cocogitto` leads the field on stars and downloads and is still excluded, which is the prompt's ordering applied correctly. `committed`'s adopters are the evidence of standing (`clap`, `ratatui`, `env_logger`, `toml`, `typos`), and the author's Cargo-team membership is cited from the governance roster.

**Citations I re-verified on 2026-09-05 at the `v1.1.11` tag.** Workspace `Cargo.toml`: `license = "MIT OR Apache-2.0"`, `edition = "2024"`, `rust-version = "1.89"`. `crates/committed/Cargo.toml`: one feature `unstable-schema = ["dep:schemars"]`; `git2 = { version = "0.20", default-features = false }`; `anyhow = "1.0"`; no async runtime. `checks.rs:92-97` runs the subject and line checks only when the limit is non-zero; `:98-100` dispatches `check_hard_line_length` with `config.line_length()` (the case-17 defect); `:129-131` and `:156-158` measure the soft line up to `rfind(' ')`. `main.rs:199-207` builds the `ignore_commit` closure from `ignore_author_re` and `:250-252` skips every check for a matching commit, which is why the revision-1 form was rejected. `.pre-commit-hooks.yaml` carries `args: [--fixup, --wip, --commit-file]` at `commit-msg`. `action/entrypoint.sh` hardcodes `VERSION=1.1.11`, downloads the release asset with `wget` and no checksum, and word-splits `${INPUT_ARGS:-}`; `action.yml` defaults to `-vv --no-merge-commit` over `HEAD~..HEAD^2`. The tag object `0a8b458c6aa66455e1f477618783ecc3f4277e7a` peels to `faeed42f2e10c244533a01525f13c4d8b6ce383f`. `releases/latest` is `v1.1.11` (2026-02-25) with the five assets the decision names. `GET https://crates.io/api/v1/crates/committed` returned `recent_downloads` 3575 and `downloads` 84770 (the decision's 3,551 and 84,746 were read at 16:00Z; the drift is consistent with elapsed time); the newest unyanked version is 1.1.11. `GET https://api.github.com/repos/crate-ci/committed`: 182 stars, not archived, `pushed_at` 2026-09-01; the search endpoint reports 22 open issues. `https://rustsec.org/packages/committed.html` returns 404 while `git2`, `anyhow` and `atty` return 200.

**The revision-3 change (gate 3) is reproduced independently.** I downloaded `committed-1.1.11.crate` from `static.crates.io`: `Cargo.toml.orig` pins `git2 = { version = "0.20", default-features = false }` and `anyhow = "1.0"`; `Cargo.lock` locks `git2 0.20.4`, `libgit2-sys 0.18.3+1.9.2`, `anyhow 1.0.100`. The advisory-db files confirm RUSTSEC-2026-0183 and RUSTSEC-2026-0184 (`git2`, `informational = "unsound"`, `patched = [">= 0.21.0"]`) and RUSTSEC-2026-0190 (`anyhow`, `informational = "unsound"`, `patched = [">= 1.0.103"]`). Greps over the published `src/*.rs` return zero occurrences of `downcast`, `unsafe`, `Remote::`, `.remote(`, `remotes(`, `blame(`, `BlameHunk` and the `Buf` type; the git2 surface used is `Repository::discover` (`main.rs:168`), `revwalk`/`push_range` (`git.rs:29-30`), `Commit::author/message/parent_count`, `as_object().short_id()` and `Oid` (`report.rs`). The reachability argument therefore holds, the `git2 = "0.20"` pin does block the `>= 0.21.0` fix until an upstream `committed` release, and the rescoped `re-verify:` trigger (vulnerability-class advisories, or `unsound` advisories in an API `committed` calls) is not fired by these three. The synthesizer's `review/evidence/committed-1.1.11-cargo-audit.log` shows `cargo audit` 0.22.1 over 181 dependencies with 0 vulnerabilities and exactly the three `unsound` warnings, exit 0.

**No re-verify trigger has fired as of 2026-09-05.** `committed` has no release beyond 1.1.11 (crates.io versions and GitHub `releases/latest`); `commitlint-rs` `releases/latest` is `v0.2.4` with an empty `assets` array; R37 and R42 are `open` in `research/CLAUDE.md`; the locked tree carries no vulnerability-class advisory.

**Disagreements are settled on evidence.** All eight rows in `## Engines` cite the evidence that settled them; row 3 concedes to Codex (commitlint does exempt URL lines) against the Opus report, and I confirmed that at `commitlint-21.0.2-comparison.log:3`. Row 4 rejects both raws' bot-exemption proposals for py's F156 shape, on the first evidence log's case R3 (a bot commit with an invalid type exiting 0 under `ignore_author_re`).

**The empirical check demonstrates the principle, not startup.** `review/evidence/committed-1.1.11-acceptance-r2.log` (34 pass, 0 fail, 5 recorded) maps each acceptance criterion to an executed case, and I checked the case lines and the fixture widths: 05 is a 116-column URL line (accepted at 72), 07 a 107-column `Claude-Session:` trailer (accepted), 08 a 96-column footer with spaces (rejected), 17 a 100-column space-free line (rejected under `hard_line_length = 200`, accepted under the recommended config), 19 a 103-column dependabot body whose subject `build(deps): bump serde_json …` carries a conventional type (so R4b passes on widths alone). H1 leaves HEAD unchanged; H2 then H3 show `--no-verify` creating the commit and the range check catching it; R3b and R4b prove the bot configuration keeps the type enum and relaxes only widths; case 16 exits 127; T1 runs the F160 adapter's four tests. The commitlint comparison is labelled supplementary and its divergences (09, 10, 11, 17, 20) match the decision's claims.

**The `audit-codex.md` rerun is consistent with the decision.** It hashes this exact revision, ran `run-checks.sh` in a fresh copy that reprovisioned the binary, and reproduced `PASS=34 FAIL=0 RECORD=5` (`review/evidence/audit-commit-message-linter-r3.log`, `RUN_CHECKS_EXIT=0`). Its exact `audit-deps.sh` rerun exited 1 because the sandbox could not take the lock on `~/.cargo/advisory-db..lock` (a read-only path); the audit text reports an isolated-cache diagnostic that reproduced the three `unsound` warnings and the zero-hit greps. The engine identities in `## Engines` match `operations/codex.json` (`gpt-5.6-terra`) and `operations/opus.json` (`actual_model` `claude-opus-5[1m]`); the raw sha256 values match the files; `scripts/check-answer-shape.sh <raw> crate` passes for both raws.

## Completeness

Required H2 sections present in order (`## Decision` line 5, `## Parameters` 190, `## Empirical check` 200, `## Engines` 246), no `## Supersedes` (correct: no accepted prior entry exists). `### Principles and implementation` (line 25) carries every element the synthesizer contract lists. The `re-verify:` line (188) is the final line of `## Decision`. `## Parameters` carries `- owns commit-message-convention = …` with the type enum, the three caps, the soft-line semantics and the bot clause, and `- assumes` lines for all four fixed parameters quoted word for word from `docs/port/PARAMETERS.md`; the prompt's `- consumes:` line is empty, so no consumed parameter is due. Neither raw raised a `CONFLICT:` line and the decision says so. The downstream note for R24 and F063/F066/F073 is present, and the registry-description alignment is flagged for the controller. Fitness gates 1-6 are each answered with evidence, gate 3 now completely. Every source carries a URL and a retrieval date (2026-09-05). Maintenance state follows the prompt's rubric.

`BASELINE-REVIEW:` and `CONFLICT:` handling: `raw/opus.md` raises neither. `raw/codex.md`'s one `BASELINE-REVIEW:` line targets F149 and F150-F157, R38's own `SUBSTITUTE` and `DIVERGENT` rows, not a `REUSE` or `ADOPT` baseline; the decision now names the line in `## Engines`, states why no controller adjudication is due under RUNBOOK §4, and disposes of its substance as disagreement #1. The two `ADOPT` rows recorded "blocked pending R38" (`docs/port/BASELINE-REVIEW.md:76,78`, verified) receive their derived values (the `.gitmessage` content and the F160 adapter) and both were executed. `## Engines` records each raw's actor, resolved model, family, sha256 and shape-check result, discloses the missing third engine with its reason and the supersession commitment, and records the revision history with hashes that match the earlier audits' `decision-sha256` fields (`3d313fbb…` rejected by revision 1's empirical audit, `563ffa5c…` approved by the second empirical rerun and rejected by the revision-2 judgment audit). The six revision-2 text observations that needed decision text (the H1 label, the `BASELINE-REVIEW:` naming, the R19 assumption, the release-please and contributors-please bypass actors, the unchecked `wget` download, the registry description) are all folded in.

## Findings

Unresolved findings: none.

Observations (no decision change required; recorded for the controller and for a future revision):

1. **The third-revision empirical audit's dependency rerun is evidenced in prose only.** `review/evidence/audit-commit-message-linter-r3.log` ends at `AUDIT_DEPS_EXIT=1` (the sandbox lock failure); the isolated-cache diagnostic that reproduced exit 0 and the three `unsound` warnings is described in `review/audit-codex.md` but its output is not in any evidence file. This is a property of the other audit's artifact, not of the decision: the decision's own `committed-1.1.11-cargo-audit.log` exits 0, and this audit reproduced the locked versions, the advisory fields and the reachability greps independently. Before `acceptance.json` cites the dependency rerun, the controller should save that diagnostic output as evidence, and `audit-deps.sh` would reproduce in a read-only-cache sandbox if it honoured `CARGO_HOME` or passed `--db` to `cargo audit`.
2. **One acceptance-criteria row overstates case 15.** The row "Missing `committed.toml` is not a silent pass" is true only for the fixture's lower-case conventional subject. Executed here against the provisioned `committed 1.1.11` in a fresh `git init` with no `committed.toml` (`--dump-config -` shows the defaults `style = "none"`, `subject_capitalized = true`, `imperative_subject = true`, the eight-member `allowed_types`): `feat: add a thing` exits 1 (`Subject should be capitalized`, the case-15 result), while `Add a thing` and `Added a thing` both exit 0. A capitalised non-conventional subject is therefore a silent pass when the file is missing. What case 15 demonstrates is that the config file is load-bearing, which the decision says elsewhere; the guards against a missing file are the F160 adapter (which fails on `read committed.toml`) and the action's `--dump-config` echo. Text only.
3. **"Billing blocked" understates the Doxa record.** `operations/doxa.json` records OpenAI `credit_balance_exhausted`, a Perplexity submit failure ("API key not found") and Gemini not attempted; "billing and auth" would be exact.
4. **Three-engine pilot policy.** RUNBOOK §2 and §6 require all three engines for R38's binding pilot, and the acceptance checker validates engine reports against `research/EXECUTION.json`. Whether acceptance may proceed on two engines is a controller and owner policy question outside this verdict; the decision commits to supersession when the third report arrives.
5. **Gate 2's dependency-tree clause was not exercised at the 1.96 floor.** Immaterial, as the decision's scope note says: the tool never enters the template's dependency graph, and CI and the release assets need no toolchain. Carried from the revision-2 audit.
6. **The illustrative CI job passes vacuously on `merge_group`.** Both steps are gated on `github.event_name == 'pull_request'`, so inside the merge queue the job reports success without linting. That is the intended F025 no-op and every commit in the queue has already been checked on its pull request; R11 owns the final shape.
7. **Tool labels in `## Engines` are not in the operation records.** `operations/codex.json` and `operations/opus.json` carry actor and model only; the "via `codex-cli-0.153.2`" and "via `claude-agent-tool`" labels come from elsewhere. Actor and model, which the acceptance schema binds, match.

## Assessment

Revision 3 resolves the one blocking finding of the revision-2 judgment audit exactly as required: gate 3 now records the `cargo audit` result over the published lockfile and the reachability argument, the `re-verify:` trigger is scoped so that the three existing `unsound` advisories do not fire it, and the upgrade gate notes that clearing the git2 advisories needs an upstream `committed` release. Everything else stands from revision 2: the principle is stated at the capability/standard level with a fit argument for the shared values, real architectural alternatives are compared before libraries, the pick follows capability and gates rather than agreement or popularity, the executed fixture proves the contract (type enum, three caps, soft-line exemption, hook rejection, `--no-verify` caught in CI, width-only bot relaxation, loud failure on a missing binary, F160 lockstep), and a second model family reproduced it. Every figure and source line I re-fetched matched.

On the absent Doxa report: it leaves no conclusion unsupported. The single-source items are the ten-issue responsiveness medians, the 258-repository `committed.toml` code-search count, the adopter enumeration for candidates that are not the pick, and commitlint's npm advisory status; none bears on the choice, which rests on source re-verification and executed behavior reproduced here. A third engine would add an independent landscape sweep, and the decision already commits to supersession when it arrives. Approve on the declared two-engine scope; the pilot-policy question in observation 4 is for the controller and owner.
