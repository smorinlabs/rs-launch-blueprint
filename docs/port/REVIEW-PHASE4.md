# Phase 4 review — independent reviewer findings and resolution (P01-TS03)

Reviewer: one agent on the most capable model, dispatched 2026-09-04 against branch `docs/p01-research-tree` at `82929d4` with the plan's eight mandatory checks (`docs/superpowers/plans/2026-09-01-p01-research-tree.md`, Task 17). It produced none of the reviewed files and edited nothing. Source repos verified at their pins: py `b08bccf`, ts `cb1cbcb`.

Reviewer preamble, verbatim: `bash scripts/check-research-tree.sh --require-owner-review` → 0; `bash scripts/test-check-research-tree.sh` → 0 (53 passed); `python3 scripts/derive-port-docs.py coverage --check` → 0; `python3 scripts/derive-port-docs.py inventories --check` → 0; citation sample = `random.seed(20260904)`, 30 of 642 `path:line` citations (py 396, ts 246); ledger = 357 rows (72 `COMMON → REUSE`, 241 `DIVERGENT`, 24 `ADOPT`, 11 `OMIT`, 5 `SUBSTITUTE`, 4 `RUST-ONLY`, 0 `OVERRIDE`).

Resolution rule (plan Task 17 Step 2): every `high` is `fixed`; `med` and `low` may be `accepted` with a reason. Fixes follow the producer rule: citations through the area file, verdicts through the ledger, then the index and prompts. Rows 21 to 51 are the reviewer's PASS records (coverage completeness and the 30 citation samples), kept as evidence.

Outcome: 1 high fixed; 7 med (5 fixed, 2 accepted); 12 low findings (10 fixed, 2 accepted); 31 PASS rows. All gates green after the fixes; ledger now 358 rows (F360 added), 25 `ADOPT`, 10 `OMIT`.

## Findings

| # | file:line | rule | finding | severity | resolution |
|---|---|---|---|---|---|
| 1 | docs/port/COMMONALITY.md:194 (+ :109, docs/port/areas/dev-experience-repo-hygiene.md:21) | §2 override labeling / check 5 | F190 "toolchain version pin-file convention" is `COMMON → REUSE` (`rust-ok: yes`) yet its Notes say "no separate pin-file needed; mechanism is `rust-version` in Cargo.toml". Both sources ship a single-file toolchain pin (`.python-version`, `.nvmrc`; D-011(3) names the discipline). `rust-version` is an MSRV floor, not a toolchain pin; the direct analogue is `rust-toolchain.toml`, which F103 (:109) assumes exists. The pattern is dropped, not reused: an unlabeled deviation with no `OV-nn`. `## Override arguments` (:365) is empty across all 357 rows; this row is the hidden override. | high | fixed — F190 Notes now name `rust-toolchain.toml` as the pin-file analogue (channel per `msrv-policy`) and `rust-version` as the separate MSRV floor; verdict stays `COMMON → REUSE`, so no OV is needed |
| 2 | docs/port/COMMONALITY.md:38 | check 5 (departure dressed as OMIT) | F032 "Multi-OS test matrix" is `OMIT` ("no Rust analogue") because the owner's `target-os-matrix` drops Windows. The fixed value `ubuntu-latest, macos-latest` is still a multi-OS matrix; the row should be `ADOPT` with the value from `PARAMETERS.md`, which already records the Windows drop as an owner override. | med | fixed — F032 re-verdicted `ADOPT` with the owner-fixed value; inventories regenerated |
| 3 | docs/port/COMMONALITY.md:279 (+ docs/port/areas/cli-framework-ux.md:18) | §3 composite split / check 5 | F275 `--no-input` is `REUSE` while its Notes admit "a real behavioral fork" (py fails the command, ts selects every result). §3: a row fitting two verdicts is composite and must be split; the area row already says "REUSE covers the flag's existence only". Which behavior Rust inherits is decided nowhere. | med | fixed — split: F275 keeps the flag (`REUSE`); new F360 (origin `different`, `DIVERGENT`, R61) in the ledger and `areas/cli-framework-ux.md`; R61's prompt cites F360 in `## Context` and asks which behavior all three repos adopt |
| 4 | docs/port/COMMONALITY.md:27 | check 5 (origin) | F021 "Cargo workspace crate topology (single vs multi-crate)" carries origin `none`, yet both sources are one package with internal layering (F020 treats package layout as having precedent). The note pre-commits ("likely the workspace crate split itself"). R02's Decision line (prompt:25) does inherit py's rules, so no departure is reopened; the origin is contestable, and a multi-crate split would be a §2 pattern change. | med | fixed (note only) — origin `none` kept: crate topology is a Rust-only mechanism and the package-layout precedent lives in F020; the Notes now say so and state that a multi-crate split would be a §2 pattern change needing an OV-nn label |
| 5 | research/topics/15-template-drift-receipt-guard/prompts/template-drift-receipt-guard.prompt.md:13, :26 | check 7 (Out of scope) | Owner direction at :10 settles rebrandability ("use template-press just the way PyLaunch Blueprint does"), but the Out-of-scope bullet (:13) still says "if undetermined, the item must state that dependency" and the first HIGH question (:26) asks whether the fact "is not yet settled". The prompt contradicts itself and spends its top HIGH on a settled fact. | med | fixed — R15 Objective, Decision line, Out-of-scope bullet and both conditional questions rewritten around the settled fact |
| 6 | docs/port/OWNER-REVIEW.md:3 | spec §6.5 vocabulary | Redefines three of four dispositions: `narrow` = "HIGH questions only, `.narrowed.prompt.md` copy" (spec: Out of scope and Questions edited to the rationale, ledger unchanged); `force` = "run even though the ledger says REUSE" (spec: prompt rewritten as a fitness check of the named tool); `drop` = "removed from the index" (spec: status `dropped`, rows re-verdicted). No operational effect today (none used), but the file is the Phase 3.5 record. | med | fixed — header now states the four effects in spec §6.5's words |
| 7 | docs/port/OWNER-REVIEW.md:5 | spec §6.5 `accept` = no tree effect | All 84 rows are `accept`, and the file says "no index or ledger row changed", yet the codesign sub-choice `b` edited 60 prompts (`## Questions`) and 21 prompts (`## Context`). Those edits are the spec's `narrow` effect under an `accept` label; the checker cannot see the difference. | med | accepted — owner decision of 2026-09-02 (recorded in the SDD ledger and now in the review header): harmonize items stay `accept` and gain one question; it is an extension of §6.5, not a `narrow`, and the header says so |
| 8 | docs/port/COMMONALITY.md:3 (all 241 `DIVERGENT` rows) | check 6 | Notes cells do not name both sides' evidence: 161 name neither py nor ts, 43 name one side, 37 name both; 1 carries a `path:line`. Of the 85 `different`-origin rows, 31 name both sides. The ledger delegates evidence to `areas/<Area>.md` by id, and traceability is 100% (every F-id resolves both ways), so the evidence is one hop away rather than absent. | med | accepted — the ledger delegates evidence to `areas/<Area>.md` by id (ledger header, spec §5); the reviewer verified traceability at 100%; duplicating 241 evidence pairs into Notes would create a second source of truth |
| 9 | research/topics/24-changelog-section-mapping/prompts/changelog-section-mapping.prompt.md:13 | check 7 (Out of scope owner) | Excludes "whether release-please itself is the changelog tool" citing D-021(3) and "decided elsewhere"; does not name F063/F066 (`REUSE`), which settle it. | low | fixed — names F063/F066 |
| 10 | research/topics/25-semver-bump-strategy/prompts/semver-bump-strategy.prompt.md:12, :13 | check 7 (Out of scope owner) | Two bullets exclude the auth mechanism, tag/version guard, OIDC publishing, and release-please itself as "already-`same`-verdict" without naming F072/F074/F075/F063. | low | fixed — names F072/F074/F075 and F063/F066/F073 |
| 11 | research/topics/26-packed-artifact-content-guard/prompts/packed-artifact-content-guard.prompt.md:13 | check 7 (Out of scope owner) | Excludes the tag/version guard and OIDC mechanism as "`same`-verdict facts" without naming F074/F075. | low | fixed — names F074/F075 |
| 12 | research/topics/23-lockfile-version-sync/prompts/lockfile-version-sync.prompt.md:12 | check 7 (Out of scope owner) | Excludes the publish tag/version guard as "a distinct decision" without naming F074 or an owning item. | low | fixed — names F074 |
| 13 | research/topics/21-contributors-bot-credential-source/prompts/contributors-bot-credential-source.prompt.md:14; research/topics/19-dependabot-config-shape/prompts/dependabot-config-shape.prompt.md:12; research/topics/06-unsafe-code-policy/prompts/unsafe-code-policy.prompt.md:13 | check 7 (Out of scope owner) | Three excluded topics name neither an owning item nor a settling row: R21 `mode: pull-request` (F060 settles it), R19 Renovate-vs-Dependabot (cites D-022(8) only), R06 dependency-tree `unsafe` audit (no owner named; R13 is the nearest). | low | fixed — R21 names F060; R19 states the bot is inherited under §2 with F053/F054; R06 names R13 |
| 14 | docs/port/OWNER-REVIEW.md:18 (e.g. research/topics/01-ports-and-adapters-seam/prompts/ports-and-adapters-seam.prompt.md:31-32) | prompt template ordering | The file says the `b` prompts carry the cross-repo question "as their last HIGH line"; in all 60 it sits after the LOW line(s), breaking the template's HIGH→MEDIUM→LOW order. No script reads question order and the RUNBOOK narrowing step ("HIGH questions only") still keeps it, so there is no functional loss; it is a defect of position and of the description. | low | fixed — the owner-review line now follows the last `- HIGH:` line in all 60 prompts; description corrected in OWNER-REVIEW.md and already accurate in RUNBOOK.md |
| 15 | research/CLAUDE.md:41-42 | index integrity | Ids jump R35 → R37. R36 (testing-coverage OpenAPI snapshot/fuzz, ex-F139/F140) was folded into F332/F334 at the Task 10 reconciliation; the only explanation lives inside those two ledger Notes, not at the index or in OWNER-REVIEW ("84 sections"). | low | fixed — one sentence in `research/CLAUDE.md` and the review header explain the vacated R36 |
| 16 | docs/port/COMMONALITY.md:139 | cross-reference | F133 Notes say "plus Cargo.lock if R25 requires sync"; lockfile sync is R23 (F065). R25 is semver-bump-strategy. | low | fixed — F133 cites R23 (F065) |
| 17 | docs/port/COMMONALITY.md:214 | stale process note | F210 Notes still read "flagging as a Phase-3.5 `narrow` disposition candidate"; the owner review chose `accept` (`b`) for R47, so the flag is resolved and the note is stale. | low | fixed — F210 Notes record the owner's `accept` (b) |
| 18 | docs/port/areas/SURVEY-PROMPT.md:1 | check 8 placeholders / check-area-file | Committed prompt template with deliberate `{{AREA}}`/`<lowercase-kebab-slug>` placeholders; `bash scripts/check-area-file.sh` fails on it (2 FAILs). Not wired into `check-research-tree.sh`, so it does not break the gate, but any future `areas/*.md` glob picks it up. Reported once. | low | accepted — the template is committed on purpose (plan: Phase 4 audits what surveyors were asked); it is outside the checker's file list, and the plan's own area-file loop is the only glob that touches it |
| 19 | docs/port/COVERAGE.md:346; docs/port/areas/web-service.md:88 | check 8 placeholders | "no new WEB-xx convention" reads as an `xx` placeholder; it is shorthand for py's `WEB-01..WEB-60` catalog (`docs/adr/0013-web-service-best-practices.md:27`). Intent is clear; wording only. The `<param>`/`<value>` text in every prompt's `CONFLICT:` and `owns` lines is answer-format instruction copied from `research/PROMPT-TEMPLATE.md:20,69`, not an unfilled placeholder. | low | fixed — `WEB-numbered` in `areas/web-service.md`; COVERAGE.md regenerated |
| 20 | docs/port/areas/ci-workflows.md:60, :66 | SURVEY-PROMPT rule 2 | Four cells (F059 py/ts, F062 py/ts) are bare `path:line` with no " — how" clause; `check-area-file.sh` did not catch it. Evidence at the cited lines is valid (see #40, #21 below). | low | fixed — all four cells carry a how clause |
| 21 | docs/port/COVERAGE.md (297 py rows = `git ls-files` 297; 122 ts rows = 122) | §6.4 / §8 | PASS: every file carries feature ids or `EXCLUDED: reason`; lockfiles excluded by one shared rule ("lockfile, generated"). All 72 `REUSE` rows carry `rust-ok: yes` and `live:`. | low | PASS — no action |
| 22 | docs/port/areas/cli-framework-ux.md:17 → ts `src/lib/logger.ts:29` | check 4 S01 | PASS: `export function resolveLevel(flags: VerbosityFlags): LogLevel {` | low | PASS — no action |
| 23 | docs/port/areas/config-env-logging.md:27 → ts `src/lib/config.ts:261` | check 4 S02 | PASS: `if (platform === 'win32') {` | low | PASS — no action |
| 24 | docs/port/areas/lint-format.md:19 → py `pyproject.toml:194` | check 4 S03 | PASS: `"__init__.py" = ["F401"]` | low | PASS — no action |
| 25 | docs/port/areas/dev-experience-repo-hygiene.md:33 → py `.github/ISSUE_TEMPLATE/01-feature-request.yml:23` | check 4 S04 | PASS: `labels: ["enhancement", "feature-request"]` | low | PASS — no action |
| 26 | docs/port/areas/static-analysis.md:16 → ts `.oxlintrc.json:23` | check 4 S05 | PASS: `"no-unused-vars": [` | low | PASS — no action |
| 27 | docs/port/areas/dev-experience-repo-hygiene.md:16 → py `Justfile:365` | check 4 S06 | PASS: `debug-info:` (recipe header; body follows) | low | PASS — no action |
| 28 | docs/port/areas/dev-experience-repo-hygiene.md:15 → ts `Makefile:77` | check 4 S07 | PASS: `install-just: ## Print install just command …` | low | PASS — no action |
| 29 | docs/port/areas/static-analysis.md:10 → py `pyproject.toml:214` | check 4 S08 | PASS: `[tool.pyright]` | low | PASS — no action |
| 30 | docs/port/areas/cli-framework-ux.md:28 → py `src/py_launch_blueprint/cli/output.py:88` | check 4 S09 | PASS: `for var in ("PLBP_PAGER", "PAGER"):` | low | PASS — no action |
| 31 | docs/port/areas/ci-workflows.md:11 → py `.github/workflows/codeql.yml:30` | check 4 S10 | PASS: `repository-visibility:` job | low | PASS — no action |
| 32 | docs/port/areas/dev-experience-repo-hygiene.md:28 → py `CLAUDE.md:7` | check 4 S11 | PASS: `@AGENTS.md` | low | PASS — no action |
| 33 | docs/port/areas/git-hooks-commit-hygiene.md:33 → py `lefthook.yml:88` | check 4 S12 | PASS: `run: uv run yamllint -c .yamllint {staged_files}` | low | PASS — no action |
| 34 | docs/port/areas/release-versioning.md:26 → ts `Justfile:234` | check 4 S13 | PASS (recipe header `@version:`; `node dist/cli.js --version` at :235) | low | PASS — no action |
| 35 | docs/port/areas/testing-coverage.md:8 → ts `tests/cli-core.test.ts:11` | check 4 S14 | PASS (import of `runCli`/`CliDeps` evidences the in-process tier) | low | PASS — no action |
| 36 | docs/port/areas/dev-experience-repo-hygiene.md:26 → py `.vscode/launch.json:8` | check 4 S15 | PASS: `"program": "${workspaceFolder}/src/py_launch_blueprint/cli/main.py"` | low | PASS — no action |
| 37 | docs/port/areas/config-env-logging.md:47 → py `src/py_launch_blueprint/web/logging.py:20` | check 4 S16 | PASS: web logging profile docstring | low | PASS — no action |
| 38 | docs/port/areas/testing-coverage.md:19 → py `Justfile:301` | check 4 S17 | PASS: `uv run --extra web pytest tests/web …` | low | PASS — no action |
| 39 | docs/port/areas/dev-experience-repo-hygiene.md:29 → ts `.windsurf/rules/justfile-rules.md:2` | check 4 S18 | PASS: `trigger: glob` | low | PASS — no action |
| 40 | docs/port/areas/ci-workflows.md:43 → py `.github/workflows/press-verify.yml:10` | check 4 S19 | PASS (workflow `name: press-verify`; triggers at :12-14) | low | PASS — no action |
| 41 | docs/port/areas/cli-framework-ux.md:29 → py `src/py_launch_blueprint/cli/options.py:101` | check 4 S20 | PASS: `click.option("--no-color", is_flag=True, …)` | low | PASS — no action |
| 42 | docs/port/areas/ci-workflows.md:60 → py `.github/workflows/publish.yml:6` | check 4 S21 | PASS (`on:` block header; `tags: ["v*"]` at :8; bare cell, see #20) | low | PASS — no action |
| 43 | docs/port/areas/git-hooks-commit-hygiene.md:28 → py `lefthook.yml:73` | check 4 S22 | PASS: `run: scripts/check-gitleaks.sh --staged` | low | PASS — no action |
| 44 | docs/port/areas/docs-system.md:11 → ts `README.md:137` | check 4 S23 | PASS (`## Documentation`; "plain GitHub-Flavored Markdown, no generated site" at :139-140) | low | PASS — no action |
| 45 | docs/port/areas/static-analysis.md:11 → py `.vscode/extensions.json:4` | check 4 S24 | PASS: `"ms-python.vscode-pylance",` | low | PASS — no action |
| 46 | docs/port/areas/ci-workflows.md:39 → py `.github/workflows/canary.yml:12` | check 4 S25 | PASS (`name: canary`; weekly cron at :16) | low | PASS — no action |
| 47 | docs/port/areas/config-env-logging.md:25 → ts `src/lib/config.ts:171` | check 4 S26 | PASS: `if (options.flagToken !== undefined && options.flagToken !== '') {` | low | PASS — no action |
| 48 | docs/port/areas/lint-format.md:27 → py `Justfile:277` | check 4 S27 | PASS: `@check: test lint typecheck lint-imports …` (no format-check, as claimed) | low | PASS — no action |
| 49 | docs/port/areas/ci-workflows.md:20 → py `.github/workflows/ci.yml:87` | check 4 S28 | PASS: `uses: astral-sh/setup-uv@v8.3.2` | low | PASS — no action |
| 50 | docs/port/areas/config-env-logging.md:14 → py `src/py_launch_blueprint/core/paths.py:108` | check 4 S29 | PASS: `def config_dirs() -> list[Path]:` | low | PASS — no action |
| 51 | docs/port/areas/web-service.md:26 → py `src/py_launch_blueprint/web/middleware.py:49` | check 4 S30 | PASS: `ACCESS_LOG_EXCLUDED_PATHS: frozenset[str] = frozenset(` | low | PASS — no action |
