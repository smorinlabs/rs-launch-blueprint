# Divergence analysis — why the source templates differ, per research item

Phase 3.5 input (owner decision 2026-09-02, `P01-T07a`). The ledger `docs/port/COMMONALITY.md` records *that* `py-launch-blueprint` (`b08bccf`) and `ts-launch-blueprint` (`cb1cbcb`) differ on a feature (its `origin` column). This document records *why*, for every research item in `research/CLAUDE.md`, and separates the Rust question from the question of whether the two source repos should themselves be brought into line.

Owner clarification A5 (2026-09-04) supersedes the original uniformity recommendations below. Source facts and historical classifications remain unchanged. The `harmonize` column now supplies comparison scope, not a required result: identify the shared principle and agreement level, then distinguish accidental drift from justified native designs. No historical suggestion of "no research" exempts an inherited mechanism from P02's baseline review.

## How to read a row

| column | meaning |
|---|---|
| item | research item `R## slug`; a `fact` cell of `item` is the item-level summary, an `F###` cell is a per-feature sub-row added only where the item's features fall in different classes |
| py state / ts state | what each source repo does, with a `path:line` citation into the pinned commit; `absent` when the repo has no such feature |
| why they differ | the recorded reason — `D-0nn` cites `ts-launch-blueprint/docs/port/TS_PORT_DECISIONS.md`, the TypeScript port's own decision log; `inferred:` marks a reason not recorded anywhere |
| class | cause class, table below |
| Rust question | the original Rust question; a historical `none` is a baseline claim to assess under A5, not an exemption from evidence |
| harmonize | original comparison scope: `yes` = all three repos; `no` = Rust alone; `partly` = shared portion plus native mechanics. Later owner choices determine the actual prompt scope; A5 determines the level of agreement |

## Cause classes

| class | meaning | typical resolution |
|---|---|---|
| A · setting drift | same mechanism in both repos, different arbitrary value (a cadence, a timeout, a threshold, a name list) | pick one value for all three repos; no research |
| B · same pattern, language-bound tool | both repos do the same thing, each with a language-specific tool — or only py has it and the spec already commits rs to the same pattern (the 15 web-service items, spec §4 D7) | Rust needs its own tool pick; research is a crate or tool search |
| C · language-bound mechanic | the pattern exists only because of one ecosystem's mechanics | decide whether an equivalent mechanic exists for Rust at all |
| D · one-sided, language-neutral | only one source has it and nothing about it is language-specific | adopt in all three or drop in all three; little or no research |
| E · one-sided, language-specific | only one source has it because only that language needed it | Rust: does the need exist? |
| F · genuine design disagreement | both viable, language-neutral; the two repos simply chose differently | decide once and apply to all three, or accept per-repo choice — owner call |
| G · Rust-only | no precedent in either source | research |

Effect on the owner review (`docs/port/OWNER-REVIEW.md`): every item keeps a disposition from the spec's vocabulary (`accept`, `narrow`, `force`, `drop`); an item with `harmonize: yes` or `partly` additionally gets one question appended to its prompt — "identify the shared principle and agreement level, then recommend and justify ecosystem-appropriate designs" — so the P02 research answers the cross-repo question at the same time.

## Summary

84 research items analysed; 165 table rows (84 item rows + 81 fact sub-rows where facts inside one item have different causes).

**Cause class × harmonize** (item level). `harmonize` records the original comparison classification; it does not prescribe identical implementation values (A5).

| class | meaning | items | harmonize yes | partly | no |
|---|---|---|---|---|---|
| A | setting drift — same mechanism, different value | 7 | 5 | 2 | 0 |
| B | same pattern, language-bound tool | 26 | 0 | 5 | 21 |
| C | language-bound mechanic | 9 | 0 | 3 | 6 |
| D | one-sided, language-neutral | 18 | 13 | 5 | 0 |
| E | one-sided, language-specific | 5 | 0 | 0 | 5 |
| F | genuine design disagreement | 13 | 4 | 5 | 4 |
| G | Rust-only, no precedent | 2 | 0 | 0 | 2 |
| mixed | facts inside the item fall in different classes — see sub-rows | 4 | 0 | 4 | 0 |
| **total** | | **84** | **22** | **24** | **38** |

**Harmonize = yes (22 items)** — the divergence is a value or an omission that is not language-bound; the owner-review prompt for each gains the question "identify the shared principle and agreement level, then recommend and justify ecosystem-appropriate designs", and propagating the answer to py and ts is a follow-on project:

R07 `contributors-bot-trigger-cadence`, R08 `workflow-permission-hardening`, R09 `self-hosted-runner-indirection`, R17 `secret-scanning-ci-workflow`, R18 `ai-assisted-review-workflows`, R20 `third-party-action-pinning-policy`, R21 `contributors-bot-credential-source`, R24 `changelog-section-mapping`, R25 `semver-bump-strategy`, R33 `property-and-snapshot-testing`, R39 `secret-scanning-hooks`, R43 `ai-assistant-repo-furniture`, R45 `pr-comment-bot-trigger-block`, R46 `per-file-license-header`, R47 `contributors-recipe-mode`, R54 `config-discovery-tiers`, R57 `xdg-directory-set`, R62 `clipboard-integration`, R63 `progress-spinner`, R65 `color-enablement-chain`, R81 `docs-delivery-model`, R85 `rich-terminal-row-niceties`

**Harmonize = partly (24 items)** — one part of the item is a shared org value, the rest is Rust mechanics; the sub-rows or the `harmonize` cell say which part:

R01 `ports-and-adapters-seam`, R02 `crate-boundary-enforcement`, R13 `dependency-vulnerability-scanning`, R14 `large-file-guard-strategy`, R19 `dependabot-config-shape`, R27 `formatter-config-surface`, R28 `linter-and-editor-tooling`, R30 `type-check-gate`, R32 `test-harness-and-execution`, R34 `scheduled-freshness-lanes`, R35 `coverage-tooling`, R37 `hook-manager-distribution`, R38 `commit-message-linter`, R40 `auxiliary-hygiene-hooks`, R44 `devcontainer-environment`, R48 `mocking-crates`, R50 `install-smoke-test`, R55 `config-error-tolerance`, R56 `config-secret-policy`, R61 `interactive-prompts`, R64 `pager-integration`, R66 `output-format-surface`, R67 `error-and-exit-code-contract`, R69 `web-framework-stack`

**Harmonize = no (38 items)** — Rust decides alone: every B, C, E and G item not listed above, plus four F items whose disagreement is a per-repo architecture choice rather than a shared value (R03 `port-absence-vs-failure-contract`, R05 `sync-async-execution-model`, R58 `logging-pipeline-architecture`, R59 `file-log-sink`).

**Effect on the owner review.** No item changes disposition because of this analysis: every item stays `accept` (research proceeds). What changes is the *question* the research answers — a harmonize-yes item asks for one value for all three repos, not a Rust-only value. The scope-level question "should ts-launch-blueprint also ship the web tier that py has and rs will have" is carried once, on R69 `web-framework-stack`, not on each of the 15 web-service items.

**Controller cross-check rulings** (recorded in the SDD ledger): the 8 web-service items R51 and R69–R75 were reclassed `F` → `B` for consistency with R76–R80/R83/R84; the 4 testing bundles R32/R34/R35/R48 carry `mixed` at item level with the class on each sub-row. Analyst reasoning for every borderline call is preserved verbatim under **Analyst notes** at the end of this document.


## Items


| item | fact | py state | ts state | why they differ | class | Rust question | harmonize |
|---|---|---|---|---|---|---|---|
| R01 ports-and-adapters-seam | item | Port/adapter split: `Protocol` port (`core/ports.py:40`), composition root wires concrete adapter (`composition.py:36`), shipped in-memory fake (`core/adapters/in_memory.py:20`), contract tests run both adapters (`tests/core/test_projects_repository_contract.py:70`) | Direct DI: `CliDeps` interface of typed functions (`router.ts:31`), `realDeps()` builds them inline (`router.ts:55`), fakes built ad hoc in `tests/*.test.ts`, no port/adapter split | no recorded reason for F001/F002/F013/F121; F011/F018 follow structurally from F001's absence in ts | F | Which parts of py's port/adapter shape (trait port, composition root, shipped fake, cross-adapter contract tests) does rs adopt, and with what Rust mechanism (dyn Trait vs generic bound)? | partly — see sub-rows |
| R01 | F001 | Port abstraction, `Protocol`: `core/ports.py:40` | No port type, direct function injection: `router.ts:31` | inferred: py separates contract (port) from impl (adapter) from use-case (service); ts injects concrete functions straight into the router with no swap seam | F | dyn Trait (runtime dispatch) or generic+trait bound (static dispatch) for the port? | no |
| R01 | F002 | Composition root binds adapter to service: `composition.py:36` | `realDeps()` builds production functions directly, no swappable backend: `router.ts:55` | inferred: follows from F001 — py's root exists to swap a live adapter for a fake; ts's root has no alternate backend to swap | F | Idiomatic Rust composition-root shape (factory fn, struct holding boxed trait object, or DI crate)? | no |
| R01 | F011 | Adapter satisfies port, verified by `ty`: `pyproject.toml:253` (HEX-33) | No ports to verify; ts's structural typing applies to `CliDeps`, not a chosen verification step | inferred: consequence of F001 — nothing to verify without a port | C | Does Rust's trait system verify this at compile time for free, with no extra tool needed? | no |
| R01 | F013 | Shipped first-class fake, not test-only: `core/adapters/in_memory.py:20` (HEX-41) | Fakes built inline per test file, none shipped under `src/` | no recorded reason (ts area notes describe the gap, no D-0nn) | D | Ship a first-class in-memory fake in the rs library crate? Needs interior-mutability support? | yes |
| R01 | F018 | Web layer stays a thin adapter reusing the CLI's data contract: `docs/adr/0013-web-service-best-practices.md:21` | absent — ts ships no web/HTTP-server capability at all (confirmed: no express/fastify/hono in `package.json`) | inferred: ts never built a web front-end, so there is nothing to compare, not a language limit | D | If rs ships a web feature, mirror py's thin-adapter-over-one-contract pattern? | yes |
| R01 | F121 | Parametrized contract suite runs every test against both adapters: `tests/core/test_projects_repository_contract.py:70` (HEX-40) (area: testing-coverage) | absent — "No TS_PORT_DECISIONS entry addresses carrying this pattern over" | no recorded reason; consequence of F001's absence in ts (no second adapter to parametrize against) | D | Contingent on R01/R02: if rs ships >1 adapter, parametrize tests across them? | yes |
| R02 crate-boundary-enforcement | item | Import-linter (`pyproject.toml:323-349`) + `tach` (`tach.toml:19`) + `ruff` TID251 (`core/ruff.toml:13`) mechanically enforce inward-only core, front-end independence, internal layering, framework-bleed, all per ADR-0017 | No boundary tool at any tier; `oxlint` loads the `import` plugin but has no boundary rules configured | no recorded reason for any row; ts's flat `src/` with one front-end and no core/front-end split leaves nothing for these tools to check (consequence of F001) | C | Does a single Rust crate with module visibility (`pub(crate)`) suffice, or does this need a genuine multi-crate workspace plus a `cargo-deny`/`tach`-like graph checker? | partly — see F021 |
| R02 | F020 | Package nested one level under `src/py_launch_blueprint/...`: `pyproject.toml:139-141` | Flat files directly under `src/`, no package-name subdirectory: `cli.ts:1` | inferred: Python's import system requires an importable package directory name; TS/Node module resolution works off file paths, no nesting required | C | Rust crates are already namespaced by `Cargo.toml` `name`; does rs need any directory nesting at all? | no |
| R02 | F021 | absent — no compiled-workspace concept | absent — no compiled-workspace concept | origin `none`: neither source repo has a compiled multi-artifact workspace to inherit from | G | Single crate vs. multi-crate workspace (`core`/`composition`/`cli`/`web`) — the central R02 decision, no precedent either side | no |
| R03 port-absence-vs-failure-contract | item | `Option`/service-raises split: `core/ports.py:47` returns `... \| None`; `core/services/projects.py:56` turns `None` into `ProjectNotFoundError` (HEX-12) | Single-function fold: `lib/api.ts:182` `getProjects` resolves and throws `NotFoundError` inline, same function as the HTTP call | no recorded reason (ts-decisions: —); inferred: py deliberately splits "is it there" (adapter) from "is absence an error" (service); ts folds both into one call site | F | Should rs's seam return `Option<T>` with a separate absence→error conversion (py's shape), or a `Result<T, DomainError>` that already distinguishes not-found from transport failure (closer to ts)? | no |
| R04 public-api-surface-enforcement | item | `__all__` documents but does not enforce: `docs/design/0005-hexagonal-architecture-and-enforcement.md:298` (HEX-34); a deep import of an internal module is never blocked | `package.json:24` `exports` map declares `.` as the only importable path; Node module resolution refuses any other subpath | D-012: both curate one file as "the only stable API surface" (`lib.ts:1-3`), but only ts's mechanism is enforced by the runtime/resolver itself | C | Does Rust's own `pub`/private-by-default visibility already give a non-bypassable guarantee equivalent to ts's `exports` map, with no separate enforcement tool needed? | no |
| R05 sync-async-execution-model | item | Sync core (`import requests`, blocking: `core/adapters/py_api.py:38`); web layer bridges into async FastAPI via its threadpool (`web/routers/projects.py:25`) | Async end-to-end: `Promise`-based from CLI entry (`router.ts:182`) through the injected `fetch` (`lib/api.ts:108`); no sync core to bridge | no recorded reason (ts-decisions: —); inferred: reflects each source ecosystem's dominant idiom (Python: sync-first HTTP client; JS/Node: async-native I/O), not a forced language limit since both languages support the other model | F | Sync core with threads (`reqwest::blocking`/`ureq`) or async core (`reqwest`+`tokio`) — cost trade-off for a CLI that is also an optional web service | no |
| R05 | F022 | absent — no async executor concept (py is sync end-to-end) | absent — no pluggable runtime; `Promise`s run on the JS event loop, not a selectable executor | origin `none`: moot in both source repos; only becomes live if F016 keeps rs's I/O boundary async | G | If rs goes async, which runtime (`tokio` vs a lighter alternative) — no precedent either side | no |
| R06 unsafe-code-policy | item | absent — no `unsafe`-equivalent construct | absent — no `unsafe`-equivalent construct | origin `none`: Python and TS have no unsafe/safe-code boundary to port from | G | `#![forbid(unsafe_code)]` vs `#![deny(unsafe_code)]` at the workspace root — Rust-only safety-boundary decision, no precedent either side | no |
| R07 contributors-bot-trigger-cadence | item | push to main (`paths-ignore`) + `workflow_dispatch` — `.github/workflows/update-contributors.yml:3` | weekly cron + `workflow_dispatch` — `.github/workflows/update-contributors.yml:49` | area notes (no `D-0nn` cited): "ts deliberately diverges from the push trigger to avoid stacking noise on top of CI/CodeQL/dependency-review" | A | none — pick one cadence (push-on-change or weekly cron) for all three repos | yes |
| R08 workflow-permission-hardening | item | hardening applied narrowly/inconsistently: `persist-credentials: false` only on the shared difftree template (F029, `difftree-pr-comment.yml:55`); contributors-bot grants perms explicitly, no deny-all (F030, `update-contributors.yml:11`) | applied broadly: `persist-credentials: false` on ci.yml/codeql.yml/publish.yml/dependency-review.yml/manual-pr-security-scan.yml (F029, `ci.yml:57`); contributors-bot uses deny-all + re-grant (F030, `update-contributors.yml:56`) | `D-022(10)`: ts "normalizes the source's declared-but-inconsistent least-privilege intent using the org's established deny-all pattern" | A | none — inherit ts's broad deny-all + `persist-credentials: false`-everywhere convention for all three repos | yes |
| R09 self-hosted-runner-indirection | item | `vars.RUNNER_*` repo vars select self-hosted (Blacksmith) with `ubuntu-latest` fallback, used in 17/21 workflows; `actionlint.yaml` declares the labels/vars (F031/F056/F057) — `.github/workflows/ci.yml:34`, `.github/actionlint.yaml:56,62` | absent — hardcodes `ubuntu-latest` everywhere, no self-hosted actionlint config | inferred from `D-022(1)`: "Blacksmith explicitly not reused for a public template" — an org/template policy, not a language constraint | D | none — pick one runner strategy (fixed `ubuntu-latest` per D-022(1), or keep the `vars.RUNNER_*` indirection) for all three repos | yes |
| R09 | F058 | `create-github-app-token` stale-metadata suppression scoped only to `release-please.yml` — `.github/actionlint.yaml:67` | scoped to `release-please.yml` AND `publish.yml`, though ts's `publish.yml` never calls `create-github-app-token` — `.github/actionlint.yaml:11` | no recorded reason; inferred: ts's suppression list over-scopes ahead of a mechanism it doesn't currently use | A | none directly — match the suppression list to whichever workflows release-versioning's chosen auth mechanism (`create-github-app-token` or not) actually touches | partly — depends on release-versioning's auth-mechanism choice |
| R10 dependency-cache-action | item | `astral-sh/setup-uv@v8.3.2` provides its own dependency cache — `.github/workflows/ci.yml:87` | `actions/setup-node@v7` with `cache: 'pnpm'` — `.github/workflows/ci.yml:69` | `D-022(3)`: setup-node's built-in cache is "the unanimous org precedent... simpler than hand-rolled actions/cache" | B | which crate/action caches cargo deps — `dtolnay/rust-toolchain` has no cache; `Swatinem/rust-cache` or `actions/cache` are the candidates | no |
| R11 ci-workflow-job-structure | item | `lint.yml` runs 5 jobs (actionlint, yamllint, bandit, codespell, editorconfig-check) with a `changes` job path-filtering the slow ones (F038, `lint.yml:15,30-64`); the same `changes` job also gates typecheck/import-boundaries/test/build-smoke (F040, `ci.yml:32`) | absent — oxlint/oxfmt/tsc run unconditionally inline inside `ci.yml`, no separate lint workflow or changes-detector | no recorded reason; inferred: py's 5 independent, slower Python-ecosystem CLI tools justify a dedicated workflow + skip-gate; ts's single fast oxlint/tsc toolchain doesn't need one | E | does Rust's chosen lint/format/typecheck toolchain end up fragmented/slow enough to need its own workflow + path-filtered skip-gate, or is it fast enough to run inline like ts? | no |
| R12 aggregate-required-status-check | item | `ci-ok` job checks `needs.*.result` across every other job (treating skipped as pass) as the single required status check — `.github/workflows/ci.yml:268` | absent — no aggregator job | no recorded reason; inferred: py's larger job count (test matrix, typecheck, import-boundaries, build-smoke, docs, toml-format, lint.yml's five) needs one aggregator to keep branch protection's required-check list short; ts's smaller job count doesn't | E | will Rust's own CI job count be large enough to warrant one aggregate required-check job, or can branch protection list jobs individually as ts does? | no |
| R13 dependency-vulnerability-scanning | item | weekly scheduled `pip-audit --strict` full-graph audit (F042, `dep-audit.yml:13`) + `pyupio/safety-action` manual scan (F048, `manual-pr-security-scan.yml:49`) | no scheduled audit; commented uncomment-to-enable `pnpm audit` scaffold in ci.yml (F049, `ci.yml:119`) + `google/osv-scanner-action` manual scan (F048, `manual-pr-security-scan.yml:74`) | mixed — see sub-rows (F048 is a language-bound tool swap; F042/F049 are a genuine strategy disagreement) | B+F (mixed) | see sub-rows | partly |
| R13 | F042 | weekly scheduled `pip-audit --strict` job against the exported locked dependency graph — `.github/workflows/dep-audit.yml:13` | absent — no scheduled audit workflow | inferred: ts substitutes a commented always-on scaffold (F049, `D-022(7)`) for continuous SCA instead of a live scheduled job — a strategy choice, not a language constraint | F | pick one continuous-SCA strategy (live scheduled full-audit job like py, or commented uncomment-to-enable scaffold like ts) for all three repos, or allow per-repo choice | yes (owner call; per-repo also acceptable) |
| R13 | F048 | `pyupio/safety-action@v1` with `secrets.SAFETY_API_KEY` — `.github/workflows/manual-pr-security-scan.yml:49` | `google/osv-scanner-action@9a49870…` (v2.3.8), no API key — `.github/workflows/manual-pr-security-scan.yml:74` | `D-022(6)`: osv-scanner is "PM/lockfile-agnostic... closest functional analog to safety while removing the secret" | B | which vulnerability-scan tool for Rust's manual PR scan — `cargo-audit`, `cargo-deny`, or `osv-scanner` (already supports crates.io)? | no |
| R13 | F049 | absent — no commented SCA scaffold; py's live `pip-audit` (F042) already covers the always-on intent | commented `pnpm audit --audit-level high` step with a same-repo fork guard, uncomment-to-enable — `.github/workflows/ci.yml:119` | `D-022(7)`: "Zero-install, no API key, no third-party action — ideal uncomment-to-enable scaffolding matching the source's executable-documentation intent" | F | same decision as F042 — one SCA strategy covers F042/F048/F049 together | yes (owner call) |
| R14 large-file-guard-strategy | item | CI-level guard: `large-file-guard.yml` rejects new files >1MB outside `docs/assets/` (F043); hook-level: lefthook `large-files` job also rejects >1MB, exempts `docs/assets/*` (F172) | no CI-level guard (F043 absent); hook-level: lefthook `check-large-files` job rejects >500KB, no exemption (F172) | mixed — see sub-rows (F043 is a one-sided redundancy question; F172 is a same-mechanism value drift) | D+A (mixed) | see sub-rows | partly |
| R14 | F043 | dedicated `large-file-guard.yml` workflow rejects new files over 1 MB outside `docs/assets/` — `.github/workflows/large-file-guard.yml:4` | absent — no CI-level large-file check, only the lefthook pre-commit hook (F172) | no recorded reason; inferred: ts relies on the hook alone and skipped CI-level redundancy | D | does Rust need CI-level redundancy on top of a pre-commit hook, or is the hook alone sufficient for all three repos? | yes |
| R14 | F172 | lefthook `large-files` job rejects staged files >1,048,576 bytes (1 MB), exempts `docs/assets/*` — `lefthook.yml:137,143` | lefthook `check-large-files` job rejects staged files >512,000 bytes (500 KB), no exemption — `lefthook.yml:42` | `D-020(6)` explains why the hook duty exists at all, not the specific threshold/exemption; no recorded reason for the value difference | A | none — pick one threshold value (and whether to exempt an assets path) for all three repos | yes |
| R15 template-drift-receipt-guard | item | Hermetic self-press verification (`.github/workflows/press-verify.yml:10`) plus committed-receipt rejection guard (`.github/workflows/template-receipt-guard.yml:6`), exercising py's custom `press` rebrand tool. | absent | no recorded reason; ts's own decision log carries no D-0xx adopting or rejecting it — inferred: the pattern only matters if the ported template is itself rebrandable, and ts's port never revisited that question. | C | Is `rs-launch-blueprint` designed to be rebrandable (e.g. via `cargo-generate`), and if so does an equivalent regen/verify mechanic exist to mirror `press verify`? | no |
| R16 codeql-config-customization | item | Custom CodeQL config selecting `security-extended` queries, ignoring `tests/**`/`docs/**` (`.github/codeql/codeql-config.yml:11`), wired via `.github/workflows/codeql.yml:63`. | absent — ts's CodeQL Init step (`.github/workflows/codeql.yml:105`) passes no `config-file:` input | no recorded reason; ts's decisions cover the CodeQL job shape (D-022(4)) but never address the config-file/query-pack question — inferred: JS/TS extractor's default set was judged sufficient. | C | Does CodeQL's Rust extractor offer a `security-extended`-equivalent pack, and does Rust's test layout need a `paths-ignore` the default analysis doesn't already exclude? | no |
| R17 secret-scanning-ci-workflow | item | Dedicated `secret-scan.yml` running TruffleHog `--only-verified`: diff-scan on PR, full scan on push (`.github/workflows/secret-scan.yml:1`). | absent | no recorded reason; ts relies solely on a hook-tier gitleaks pre-commit/pre-push check (git-hooks-commit-hygiene area) instead of a CI-tier scanner — inferred: judged redundant, not documented as such. | D | none in principle — TruffleHog (or an equivalent) is language-agnostic; the open call is whether a CI-tier scan is worth keeping alongside a hook-tier one, for all three repos alike. | yes |
| R18 ai-assisted-review-workflows | item | Two `anthropics/claude-code-action` workflows: automatic PR review skipping bot-authored PRs (`.github/workflows/claude-code-review.yml:1`) and an `@mention`-triggered assistant (`.github/workflows/claude.yml:1`). | absent | no recorded reason; ts's decision log never mentions claude-code-action — a plain omission, not a documented rejection. | D | none — the action operates on diffs/comments regardless of language; if adopted, port the trigger/skip-guard logic unchanged and re-verify the action's current version. | yes |
| R19 dependabot-config-shape | item | `.github/dependabot.yml:14` — ecosystems `uv`, `github-actions`, `npm` (Bun-only); `.github/dependabot.yml:28` — four named groups, 5-day cooldown, labels, commit-message prefix. | `.github/dependabot.yml:28` — ecosystems `github-actions`, `npm` (covers pnpm); `.github/dependabot.yml:34` — one minor/patch group per ecosystem, no cooldown/labels. | D-022(8): Dependabot chosen as GitHub-native, zero-infrastructure automation; grouping deliberately simplified to one group per ecosystem rather than porting py's richer scheme. | A | Does Dependabot's `cargo` ecosystem support the same `groups`/cooldown keys py uses, and is py's richer grouping worth the added config for a template's smaller dependency surface? | partly — grouping-strategy shape can be one org value; the `cargo` ecosystem key itself is Rust-specific |
| R20 third-party-action-pinning-policy | item | Floating major-version tag on third-party actions, e.g. `smorinlabs/contributors-please-action@v1.3.9` (`.github/workflows/update-contributors.yml:27`). | Full commit SHA plus version comment on the same action (`.github/workflows/update-contributors.yml:74`). | D-022(9): hybrid pinning policy — SHA-pin third-party/security-sensitive actions, "kept fresh by Dependabot's github-actions ecosystem," following the org's newest precedent (difftree-action). | F | none — both are viable, language-neutral supply-chain postures; pick one policy for the org and apply identically to py, ts, and rs. | yes |
| R21 contributors-bot-credential-source | item | PAT-fallback token from a dedicated repo secret `secrets.CONTRIBUTORS_PLEASE_PAT` (`.github/workflows/update-contributors.yml:34`), requiring maintainer setup. | Ambient `secrets.GITHUB_TOKEN` (`.github/workflows/update-contributors.yml:80`), no extra secret needed. | Recorded in the area notes (not a numbered D-022(k) item): "ts's choice lets the template work with zero extra repo secrets configured" — trades away GITHUB_TOKEN's inability to trigger downstream workflows for zero-setup friction. | F | none — both are language-neutral; decide once whether the org values zero-config friction or the PAT's downstream-trigger capability, and apply that choice to rs too. | yes |
| R22 runtime-version-accessor | item | `src/py_launch_blueprint/__init__.py:24` — `importlib.metadata.version()` reads installed-package metadata at runtime | `src/version.ts:6` — imports `package.json`'s `version`, inlined by the bundler at build time | no recorded reason (D-021(2) only justifies ts's own choice); inferred: Python has no build step to inline into, runtime lookup is the only mechanism; JS bundlers do static inlining | B | Does `env!("CARGO_PKG_VERSION")` (compile-time, ts-like) fully substitute for py's runtime lookup, or does the web target need a runtime value? | no |
| R23 lockfile-version-sync | item | `release-please-config.json:16` — `extra-files` jsonpath bumps `uv.lock`'s editable-root version entry | `.github/workflows/release-please.yml:27` — comment: pnpm-lock.yaml has no root version field, nothing to sync | no D-0nn cited; inferred: purely a lockfile-schema difference — `uv.lock` encodes the root package's own version, `pnpm-lock.yaml` does not. `Cargo.lock` does encode workspace-member versions (area notes), so the mechanic exists for Rust like it does for py | C | Does release-please's `extra-files` jsonpath work against `Cargo.lock`'s TOML, or does syncing it need a companion `cargo update -p <member>` step in the release workflow? | no |
| R24 changelog-section-mapping | item | `release-please-config.json:23` — feat/fix/perf/refactor/revert/deps visible; chore/docs/style/test/ci/build hidden | `release-please-config.json:12` — feat/fix/perf/docs/test/refactor visible; chore hidden | D-021(3): ts's release-please `changelog-sections` config ports its *source* repo's `cog.toml` (cocogitto) mapping, not py's release-please mapping — the divergence is inherited from a different upstream precedent, not a fresh py-vs-ts design call | A | Which Conventional-Commits types should be visible for a Rust CLI+library+web template's own audience? No tooling gap — pick one list. | yes |
| R25 semver-bump-strategy | item | absent — py's `release-please-config.json` has no `bump-minor-pre-major`/`bump-patch-for-minor-pre-major`; py was already post-1.0 (2.4.2) at port time | `release-please-config.json:6` — `bump-minor-pre-major`/`bump-patch-for-minor-pre-major` both `true`; ts started fresh at 0.1.3 | D-021(1) (adjacent): ts's flags reflect that ts launched pre-1.0 with no prior consumers; py's absence of the flags reflects py already being past 1.0, not a rejection of the pattern — inferred: circumstantial (each project's actual maturity at port time), not a language constraint | D | `rs-launch-blueprint` is a brand-new crate like ts was — does release-please support both flags identically for a Rust/generic `release-type`, and should rs start pre-1.0 like ts did? | yes |
| R26 packed-artifact-content-guard | item | No packed-artifact file-list assertion; py's closest analogue is a build-smoke job (`uv build`, install wheel/sdist, run CLI) — a different mechanism, not a content guard. | `F039` — `npm pack --dry-run --json` parsed to assert dist-plus-whitelist contents (`.github/workflows/publish.yml:112`). `F078` — publish job fails if npm silently auto-corrects the `bin` field (`.github/workflows/publish.yml:199`). | D-021(5): ts's publish `verify` job runs a pack dry-run as part of its OIDC publish pipeline design; the `bin`-field guard exists because npm packaging is documented to silently rewrite that field. | C | Does `cargo package --list`/`cargo publish --dry-run` give a diffable file list, and does Cargo have any analogous silent-metadata-rewrite behavior to guard against, or is that half a non-issue for Rust? | no |
| R27 formatter-config-surface | item | one dedicated formatter (`ruff format`) via Justfile/lefthook/CI, 88-col, LF auto-detect, isort-as-lint-rule, shared `[tool.ruff]` exclude, blocking pre-commit; `Justfile:203`, `pyproject.toml:148` | `oxfmt` (0.58 beta, exact-pinned) via same three surfaces, printWidth 100, LF fixed, `sortImports` formatter option, per-tool `ignorePatterns`, writes+restages; `Justfile:105`, `.oxfmtrc.json:6` | D-014(1): oxfmt beta uniquely formats YAML/TOML/MD passing 100% Prettier conformance, preserving py's one-fast-tool intent; config values (D-014(2)/(3)) reused from agent2linear/POC precedent, not re-derived | B | rustfmt is near-uncontested (bundled via rustup) — confirm no viable alternative, then pick each config value (max_width, newline_style, trailing_comma, import ordering), noting which need nightly `unstable_features` | partly |
| R27 | F084 | `ruff format` via `uv run`; `Justfile:203` | `oxfmt` via `pnpm exec`, 0.58.0 beta exact-pinned; `Justfile:105` | D-014(1): Oxfmt beta passes 100% Prettier JS/TS conformance and formats YAML/TOML/MD, uniquely preserving one-fast-tool intent; Biome 2.5 still can't format those | B | confirm rustfmt is uncontested (bundled via rustup) with no credible dprint/Biome-style alternative worth naming | no |
| R27 | F087,F089,F092,F100,F102,F104 | line-length 88 (`pyproject.toml:148`), line-ending auto (`:209`), imports via ruff `I` rule (`:153`), shared `[tool.ruff]` exclude (`:180`), `check` composite omits format-check (`Justfile:277`), pre-commit blocks (`lefthook.yml:114`) | printWidth 100 (`.oxfmtrc.json:6`), endOfLine lf (`:10`), `sortImports` formatter option (`:13`), per-tool `ignorePatterns` (`.oxlintrc.json:69`), `all` composite includes format-check (`Justfile:178`), writes+restages (`lefthook.yml:32`) | D-014(2)/(3): values reused verbatim from agent2linear/POC Prettier+Biome config rather than re-derived; 88-col is a Python norm, not portable intent; no reason recorded for the F102/F104 split beyond D-014's overall shape | A | none needing crate research — pick a Rust-idiomatic value per setting in `rustfmt.toml`/Justfile | yes |
| R27 | F103 | `ruff>=0.1.0` floating range, pinned via `uv.lock`; `pyproject.toml:78` | `oxfmt` exact-pinned; `package.json:55` + `pnpm-lock.yaml` | D-014(1): Oxfmt's beta status is why it alone is exact-pinned, unlike stable-1.x Oxlint's caret range | A | inferred: doesn't carry over structurally — rustfmt ships as a rustup component, not a Cargo dependency, so this may collapse to a `rust-toolchain.toml` component pin instead | partly |
| R27 | F091 | absent — ruff's formatter has no configurable trailing-comma option (area notes) | `trailingComma: "es5"`; `.oxfmtrc.json:9` | D-014(2); inferred: tool capability gap (ruff formatter lacks the option), not that Python syntax lacks trailing commas | D | does rustfmt's `trailing_comma` exist on stable, or only behind `unstable_features`/nightly? if stable, fold into F084's settings; if nightly-only, omit like py | no |
| R27 | F162 (git-hooks-commit-hygiene) | absent — no consistency test | `tests/repo-hygiene.test.ts:81` asserts lefthook `exclude` sets equal `.oxfmtrc.json`/`.oxlintrc.json` `ignorePatterns` | no D-0nn cited; inferred: ts added the test because F100 splits excludes across per-tool configs, a drift risk py's single shared `[tool.ruff]` exclude doesn't have | D | only needed if F100's Rust resolution splits rustfmt/clippy excludes into separate files; if one shared list, the test is unnecessary | partly |
| R28 linter-and-editor-tooling | item | `ruff check` (uv run) selects rules via `lint.select` code prefixes, `fix=true` autofixes by default, `per-file-ignores` for tests/`__init__.py`, `S`(bandit) rules, Pylance extension for typecheck; `Justfile:228`, `pyproject.toml:150` | `oxlint` (pnpm exec) selects via `categories`+`plugins`/`rules`, `--fix` opt-in, `overrides` globs for tests, built-in rules+CodeQL layered, no typecheck extension (built-in), one-key `settings.json` pins `typescript.tsdk`; `Justfile:123`, `.oxlintrc.json:15` | D-014(1) tool pick mirrors F084's; D-014(4)/(5) adapt py's per-file-ignore/security intent structurally; D-032 adds a narrow tsdk pin py never needed | B | confirm clippy uncontested; pick lint-selection idiom (`[lints.clippy]` in Cargo.toml vs `clippy.toml`), autofix-default policy, test-lint relaxation shape, and whether a settings.json pin is needed for rust-analyzer | partly |
| R28 | F085,F086,F094,F096,F101 | `ruff check` lints via `lint.select` codes (`pyproject.toml:150`); `S` bandit rules (`:161`); `per-file-ignores` for tests (`:195`); `ruff` VS Code extension (`.vscode/extensions.json:5`) | `oxlint` lints via `categories`+`plugins`/`rules` (`.oxlintrc.json:15`); built-in security-adjacent rules, no bandit analog (`:35`); `overrides` globs for tests (`:52`); `oxc.oxc-vscode` extension (`.vscode/extensions.json:3`) | D-014(1) tool pick; D-014(4) overrides is a structural equivalent of per-file-ignores; D-014(5) security coverage layered with CodeQL since no bandit analog exists; D-024(4) extension follows tool pick | B | clippy's lint-group + `[lints.clippy]`/`clippy.toml` idiom for rule selection; `#[allow]`/module-scoped override for test relaxation; does CodeQL cover clippy's missing bandit-analog gap | no |
| R28 | F093 | `fix = true` makes autofix the default; `pyproject.toml:177` | `--fix` flag required; plain `lint` is check-only; `Justfile:130` | no D-0nn cited distinct from D-014's overall shape; inferred: no recorded reason for this specific policy split | A | is `cargo clippy --fix` safe enough to default-on, or does its fix suite carry enough risk to stay opt-in like ts's `--fix`? | yes |
| R28 | F109 (static-analysis) | `ms-python.vscode-pylance` recommended for the IDE-only pyright checker; `.vscode/extensions.json:4` | absent — TypeScript language service ships built into VS Code, no extension recommended | area notes: ts's built-in language service replaces the need for a separate type-checker extension that Python's IDE tooling requires | E | rust-analyzer bundles diagnostics/type info like TS's built-in service — confirm no separate extension is needed, mirroring ts not py | no |
| R28 | F195 (dev-experience-repo-hygiene) | `"type": "python"`, `program: src/py_launch_blueprint/cli/main.py`; `.vscode/launch.json:6,8` | `"type": "node"`, `runtimeExecutable: tsx` against `src/cli.ts`; `.vscode/launch.json:6,8` | D-024(3): built-in `vscode-js-debug` replaces the Python debugger; bare+args+dist/ configs ported | B | presumption-of-reuse (F194 settles the launch.json pattern) — pick the Rust debugger extension (CodeLLDB vs rust-analyzer's bundled support) | no |
| R28 | F196 (dev-experience-repo-hygiene) | absent | `"typescript.tsdk": "node_modules/typescript/lib"`; `.vscode/settings.json:2` | D-032: pins the editor's TS engine to the workspace version so editor and CI share one engine, reconciling D-015(2)'s no-second-checker argument | C | does rust-analyzer need an equivalent pin, or does its `rust-toolchain.toml` resolution make a `settings.json` pin unnecessary (prompt's own LOW question leans "no equivalent pin needed") | no |
| R29 non-code-file-formatting | item | `taplo` formats/checks TOML wired to hooks+CI (`.taplo.toml:23`); `yamlfmt` configured but unenforced (`.yamlfmt:1`); no JSON/MD formatter | no separate TOML tool (zero `.toml` files survived the port); `oxfmt` formats YAML+JSON+MD, wired into hook glob (`lefthook.yml:24`) | D-014(6)/(7): taplo dropped because no `.toml` files remained (a release-tooling consequence, not a capability gap), and yamlfmt consolidated into oxfmt to remove a go/make-install bootstrap | B | Rust's own `Cargo.toml`/`rustfmt.toml` make TOML formatting unavoidable — is there a Rust-native `oxfmt`-equivalent covering TOML+YAML+JSON+MD in one tool, or must Rust split across per-format tools like py did | no |
| R29 | F097 | `taplo` wired to hooks/CI; `.taplo.toml:23` | absent — zero `.toml` files tracked; no TOML-specific `.oxfmtrc.json` setting | D-014(6): omitted only because no `.toml` survived the port (a release-tooling/cog.toml consequence), not because oxfmt lacks TOML support — area notes verified this | D | Rust's Cargo.toml/rustfmt.toml guarantee `.toml` files exist, unlike ts — is `taplo` (cargo-installable per the prompt's own check) the pick, independent of py/ts precedent | no |
| R29 | F098 | `yamlfmt` configured, not wired to hooks/CI; `.yamlfmt:1` | `oxfmt` formats YAML, included in hook glob; `lefthook.yml:24` | D-014(7): consolidating YAML into the main formatter removes a go/make-install bootstrap while keeping every config format machine-formatted | B | what formats YAML in the Rust ecosystem (GH Actions workflows, lefthook.yml) — a maintained Rust-native YAML formatter, a cross-ecosystem tool (accepting a non-Rust dependency like py's Go binary), or unenforced like py | no |
| R29 | F099 | absent | `oxfmt` formats JSON/Markdown, included in hook glob; `lefthook.yml:24` | no D-0nn cited beyond D-014(7)'s general consolidation; inferred: ts included JSON/MD because oxfmt already supports them at no extra cost, py's ruff-based toolchain never could | D | does a Rust CLI+library+web template's actual JSON/MD footprint (Cargo.lock, README, docs/, CHANGELOG) justify a dedicated formatter dependency, or is coverage skippable | yes |
| R30 type-check-gate | item | `ty` is the CI-authoritative checker (`Justfile:237`); a second, IDE-only `pyright strict` runs uninvoked in CI (`pyproject.toml:214-218`); rule-based strictness (`pyproject.toml:254-258`); named-rule ignore discipline (`pyproject.toml:255-257`); separate warn tier (`pyproject.toml:263`); gate scoped to `src/` only (`Justfile:237`) | `tsc --noEmit` is the sole checker, serving both CI and the editor via one `tsconfig.json` (D-015(1)); no second engine, closed instead by a `.vscode/settings.json` `typescript.tsdk` pin (D-015(2), D-032); flag-based strictness (`tsconfig.json:11-18`, D-013(2)); no ignore-comment convention; no separate warn tier; gate includes `tests/**/*.ts` (D-013(4)) | D-015(1)/(2): ty is TS's mypy-equivalent gate: pyright's IDE role dissolves because tsc and the editor's language service are the same engine reading the same config, closed by the tsdk pin (D-032); D-013(4): ts deliberately widened scope to fix py's "under-checking of tests" | C | `cargo check`/rustc type-checks at compile time by construction — does rs need any distinct named tool at all beyond deciding hook tier and gate scope (`--all-targets`)? | partly — see sub-rows |
| R30 | F106 | `ty` runs at pre-push only, full-tree scan judged too slow per-commit: `lefthook.yml:166-167` (ADR-0018) | `tsc --noEmit` runs at pre-commit, every commit: `lefthook.yml:60-61` (D-020(6): folded into lefthook's staged pre-commit duties) | py: explicit speed trade-off (ADR-0018); ts: D-020(6) bundles it into pre-commit alongside staged lint/format because it was judged fast enough | A | Is `cargo check` (incremental, `target/`-cached) fast enough for pre-commit, or does it need to stay pre-push like py's `ty`? | yes — pick one tier once rs measures its own check speed |
| R30 | F111 | Every `# ty: ignore` must name its rule, enforced by `ty` itself: `pyproject.toml:255-257` | No configured `@ts-expect-error`/`@ts-ignore` linting convention | no recorded reason; inferred: an omission, not a TS/tooling limitation (eslint/oxlint rules for this exist) | D | Rust's `#[allow(lint_name)]` syntax already requires naming the lint — likely moot; confirm no equivalent "blanket ignore" gap exists for rs | yes — adopt named-suppression discipline (may already be free) |
| R30 | F113 | Gate scoped to `src/` only, `tests/` excluded from the CI-authoritative check: `Justfile:237`, `lefthook.yml:167`; `tests/` coverage exists only in the non-gating `pyright` config | Gate includes `src/**/*.ts` and `tests/**/*.ts` together: `tsconfig.json:32` | D-013(4): ts frames this as deliberately fixing py's "under-checking of tests" | A | Does rs's gate use `cargo check --all-targets` (covers tests by default) rather than reproducing py's narrower `src/`-only scope? | yes — follow ts's recorded fix |
| R31 standalone-security-analyzer | item | Dedicated AST security scanner `bandit`, run pre-push and in CI: `.github/workflows/lint.yml:114`, `pyproject.toml:267`, `lefthook.yml:160-161` | absent — no bandit-equivalent tool; security coverage is oxlint built-ins plus CodeQL only | D-014(5): ts explicitly recorded the gap rather than closing it — no TS-ecosystem AST security scanner found comparable to bandit | E | Does Rust's memory-safety guarantees plus clippy's security-adjacent lints reduce or eliminate the need bandit addresses (eval/exec, weak crypto, hardcoded secrets), or does `cargo-audit`/`cargo-deny` (dependency-CVE-focused) better fit as the actual analogue? | no |
| R32 test-harness-and-execution | item | pytest stack: runner, coverage, xdist, randomly, timeout, markers, autouse fixture, single in-process CLI tier (`Justfile:260`, `pyproject.toml:97,99,277-278,284`, `tests/conftest.py:16`) | Vitest 4 stack: runner+coverage, two-tier CLI tests, flat file layout, no markers/autouse (`Justfile:144`, `tests/cli-core.test.ts:11`, `tests/e2e.test.ts:94`) | mixed — runner swap is a tool pick (D-019); several py pytest-plugin features are simply unconfigured in Vitest; two facts are deliberate ts departures (D-019(4),(5)) | mixed — see sub-rows | see sub-rows; only F117/F131/F132 need real crate research, the rest are settings or convention picks | partly |
| R32 | F117 | `Justfile:260` — `uv run pytest` via `just test` | `Justfile:144` — `pnpm exec vitest run` via `just test` | D-019(1): Vitest is org-unanimous across recent TS repos; not a pytest-compatible runner, a wholesale replacement | B | `cargo test` vs `cargo-nextest` as base runner — does nextest natively give F124/F125 or need config | no |
| R32 | F118 | `tests/cli/test_pylb.py:9` — single in-process Click `CliRunner` tier only | `tests/cli-core.test.ts:11` in-process + `tests/e2e.test.ts:94` subprocess-on-built-binary tier | D-019(4): no Node `CliRunner` analog; the subprocess tier is "triple-precedented org DNA," not a language necessity | D | adopt ts's two-tier pattern (in-process fn call + built-binary subprocess) for Rust CLI tests, or stay single-tier | yes |
| R32 | F124 | `pyproject.toml:99` — `pytest-randomly` reorders every run, replayable seed | absent; Vitest supports `sequence.shuffle` but ts leaves it unconfigured | no recorded reason; inferred: an unconfigured Vitest setting, not a tool gap | D | does `cargo-nextest` (or `cargo test`) offer an equivalent seeded-shuffle flag worth enabling | yes |
| R32 | F125 | `pyproject.toml:284` — `timeout = 60` via `pytest-timeout` | absent; no custom `testTimeout` set in `vitest.config.ts` | no recorded reason; inferred: an unconfigured Vitest setting, not a tool gap | D | does nextest's `slow-timeout`/`terminate-after` config give per-test timeout enforcement natively | yes |
| R32 | F128 | `tests/conftest.py:16` — `@pytest.fixture(autouse=True)` resets root logger globally | absent by design; local factory helpers instead of a global setup file | D-019(5): DI-first design deliberately avoids implicit global state to keep test seams explicit/hermetic | F | does Rust need any global `#[ctor]`/setup-file equivalent, or do local builder/factory helpers suffice | yes |
| R32 | F129 | `Justfile:301` — `tests/{cli,core,meta,web}` mirrors `src/` layer boundaries | `tests/api.test.ts:4` — flat `tests/*.test.ts`, 7 files, no subdirs | D-019(2): matches source `testpaths=["tests"]` intent, and follows claim-npm's org precedent over agent2linear's colocation minority | F | pick a `tests/` layout for Rust (flat vs. src-mirroring subdirs) | partly |
| R32 | F130 | `pyproject.toml:277-278` — `markers=["live","slow"]`, excluded from default `addopts` | absent (no ts-decisions entry); tiering achieved by file split instead, not by tag | no recorded reason; inferred: pytest's marker/plugin architecture has no direct Vitest analog, so ts substitutes file-level tiering | C | `#[ignore]`+`--ignored`, a cfg-gated feature, or a crate (`rstest`/`test-case`) for opt-in slow/live tagging | no |
| R32 | F131 | `pyproject.toml:97` — `pytest-xdist`, `-n auto` opt-in flag needed because pytest defaults to serial | absent; Vitest already parallelizes test files by default, no flag needed | inferred: the flag exists only because pytest itself is serial-by-default; Vitest's (and cargo's) tool default already parallelizes | E | none — `cargo test` already parallelizes test binaries by default; no crate/flag needed | no |
| R32 | F132 | absent | absent | none — Rust-only; `cargo test --doc` runs doc-comment examples by default, no source-repo convention to port | G | should the CI-authoritative gate include `cargo test --doc` alongside unit/integration tests | no |
| R32 | F173 (git-hooks-commit-hygiene) | absent; py's `lefthook.yml` has no general test-suite hook at any tier to move from | `lefthook.yml:78` — opt-in `pre-push` `test` job (`TS_PROJECTS_PREPUSH_TESTS=1`), default skip | D-020(3) frames this as "moving" py's pytest-in-pre-commit hook, but the area note flags that framing as inaccurate — py has no such hook here | D | add an opt-in, default-skip pre-push `cargo test` hook mirroring ts's env-var gate | yes |
| R33 property-and-snapshot-testing | item | Hypothesis `@given` round-trip tests (`tests/core/test_properties.py:24`); syrupy CLI `--help` snapshots (`tests/cli/test_help_snapshots.py:36`) | absent both; no decision entry addresses omitting Hypothesis/fast-check or a `.ambr`/`toMatchSnapshot` equivalent | no recorded reason for either; inferred: both patterns are portable (Vitest has native `toMatchSnapshot`, JS has `fast-check`) — ts simply didn't adopt them, not blocked by language | D | pick a Rust property-testing crate (`proptest`/`quickcheck`) and a snapshot crate (`insta`) for parity | yes |
| R34 scheduled-freshness-lanes | item | weekly canary workflow (F126) is py-only; advisory alt-runtime lane (F127) is ts-only | see sub-rows — the two facts split into different classes | mixed: F126 is an unexplained ts omission of a portable CI pattern; F127 is ts's own recorded, JS-runtime-specific decision | mixed — see sub-rows | see sub-rows | partly |
| R34 | F126 | `.github/workflows/canary.yml:16,47` — weekly cron, `uv sync --upgrade` then test | absent; no `canary.yml` or equivalent scheduled workflow | no recorded reason; inferred: a portable CI pattern (schedule + dependency-upgrade + test) that ts simply never added | D | add a weekly `cargo update` + `cargo test` scheduled workflow, non-blocking to the merge gate | yes |
| R34 | F127 | absent | `Justfile:169` — `bun run vitest run --exclude tests/e2e.test.ts`, advisory/non-gating (D-036) | D-036: user-selected optional Bun dev/test lane, scoped to advisory-only because Bun tracks ~1 Node major behind the `engines.node` floor | C | Rust has no competing-runtime concept; the closest analogue is an advisory nightly/beta toolchain job — is that worth the ci-cost given Rust's own stable/beta/nightly cadence already exists | no |
| R35 coverage-tooling | item | pytest-cov/coverage.py, no exclusions, external Codecov gate (`target: auto`/`80%`), 3 local report formats | `@vitest/coverage-v8`, 2 files excluded, in-repo `95/95/90/95` thresholds not enforced in CI, default reporters | mixed: tool pick is B; exclusions/report-formats are arbitrary settings (A); threshold *location* and *enforcement* are a genuine, still-unresolved design split (F) | mixed — see sub-rows | see sub-rows | partly |
| R35 | F134 | `pyproject.toml:76` — `pytest-cov` wraps `coverage.py` | `package.json:53` — `@vitest/coverage-v8` (D-019(1), same version pin as the runner) | same coverage-collection pattern, ecosystem-native tool | B | `cargo-llvm-cov` vs `cargo-tarpaulin` — instrumentation mechanism, accuracy, CI-cost on `ubuntu-latest`/`macos-latest` | no |
| R35 | F135 | `Justfile:267` — `--cov=py_launch_blueprint`, no per-file omit list | `vitest.config.ts:10,19` — `include: src/**/*.ts`, excludes two thin I/O-adapter files, with comments | D-019(3): ts judges those two files not meaningfully unit-coverable; py excludes nothing | A | should Rust exclude thin I/O-adapter/entry files from coverage scope, matching ts's convention | yes |
| R35 | F136 | `.codecov.yml:14,18` — external service config: `target: auto` (project), `80%` (patch) | `vitest.config.ts:20` — in-repo `thresholds` 95/95/90/95 lines/fn/branch/stmt | D-019(3): py's gate lives in an external service; ts's lives in the test-runner config itself — a location choice, not a values-only difference | F | local `cargo-llvm-cov` threshold flag vs. Codecov (tokenless mode exists for public repos) — owner call on whether to depend on an external service | no |
| R35 | F137 | `.github/workflows/ci.yml:193` — `codecov-action` uploads; external Codecov check blocks the PR | `.github/workflows/ci.yml:95` — `just test` runs with no `--coverage`; Codecov-upload step commented out | D-022(1): ts keeps codecov/security/publish scaffolding "commented" — configured-but-inert, not currently wired to gate | F | should Rust's coverage gate actually block CI (like py) or ship as inert/uncomment-to-enable scaffolding (like ts) | no |
| R35 | F138 | `Justfile:267` — explicit `--cov-report=term-missing --cov-report=html --cov-report=xml` | absent explicit selection; Vitest's default reporters used as-is | no recorded reason; inferred: py explicitly picked 3 formats, ts just didn't override the tool default | A | pick output formats for the chosen Rust coverage tool (e.g. terminal summary + `lcov`/`html` for local, `lcov`/`json` for CI) | yes |
| R37 hook-manager-distribution | item | `scripts/install-lefthook.sh:13,35` — global `bun install -g lefthook@2.1.8` pinned outside uv; `Justfile:349` runs `lefthook install` explicitly inside `just setup` | `package.json:54` — `lefthook` `^2.1.10` devDependency; `package.json:36` `"prepare": "lefthook install"` auto-runs on `pnpm install` | D-020(1): npm/pnpm can list a precompiled binary as a devDependency and auto-run a lifecycle hook on install; uv and Cargo have no such path | C | Cargo can't install lefthook as a dependency and has no `prepare`-equivalent hook. Pick a distribution mechanism and a manual wiring trigger | partly |
| R37 | F174 | absent | `.github/workflows/ci.yml:106` — `pnpm exec lefthook run pre-commit --all-files` step inside the `ci` job | Ledger: F174 is a "resolution target" for `ci-workflows`'/lint-format's forward-referenced row; the real choice is owned by `ci-job-structure` (R11)/D-022(1) | F | none for this item — decided by R11/ci-job-structure | yes |
| R38 commit-message-linter | item | `lefthook.yml:65` — `commit-msg:` job runs `bun ./node_modules/@commitlint/cli/cli.js`; `commitlint.config.mjs:15` `extends: ['@commitlint/config-conventional']` | `lefthook.yml:68` — `pnpm exec commitlint`; `commitlint.config.mjs:26` same `extends` | D-020(2): commitlint is the org's only cross-repo precedent for this contract; both repos already pull in a JS runtime to run it — neither has a native tool today | B | No native-Rust Conventional-Commits linter has org precedent (`committed`, `cocogitto cog check`); decide JS-runtime-via-commitlint vs a native crate | partly |
| R38 | F150 | `lefthook.yml:65` — explicit `bun ./node_modules/@commitlint/cli/cli.js --edit {1}`, avoiding PATH/script fallback | `lefthook.yml:68` — `pnpm exec commitlint --edit {1}` (package-manager-resolved binary) | py's own comments (`lefthook.yml:18-58`) document three prior silent-pass failure modes that motivated the explicit-path form; ts trusts `pnpm exec`'s resolution instead | C | Only relevant if Rust also runs commitlint via a provisioned JS runtime (F149); the explicit-path-vs-PM-exec distinction is an npm/bun invocation mechanic with no Cargo analogue, and is otherwise owned by `package-manager-invocation` (R42) | no |
| R38 | F152 | `commitlint.config.mjs:15` — no `header-max-length` override (inherits config-conventional's 100-char default) | `commitlint.config.mjs:29` — `'header-max-length': [2, 'always', 50]` | inferred: D-020(2) frames the port as reconciling a "50/72 contract" attributed to the source, but py at this pinned commit does not actually override `header-max-length` — see Notes | A | none — pick one header length cap for all three repos | yes |
| R38 | F153 | `commitlint.config.mjs:17` — `'body-max-line-length': [2, 'always', 200]` | `commitlint.config.mjs:30` — `'body-max-line-length': [2, 'always', 72]` | D-020(2)/py inline rationale: py raises the default cap to fit doc URLs/permalinks; ts deliberately tightens to a stricter 72-char wrap | A | none — pick one body line-length cap for all three repos | yes |
| R38 | F154 | `commitlint.config.mjs:18` — `'footer-max-line-length': [2, 'always', 200]` | absent (inherits config-conventional's 100-char default) | inferred: py raises the footer cap for the same doc-URL reason as F153's body cap; ts never overrides it, so it silently keeps the 100-char default — no ts rationale recorded | A | none — pick one footer length cap (or accept config-conventional's default) for all three repos | yes |
| R38 | F155 | `commitlint.config.mjs:15` — no `type-enum` override (inherits config-conventional's 11-member default list) | `commitlint.config.mjs:28,11` — explicit `'type-enum': [2, 'always', types]` with an 11-entry array | D-020(2): ts's list adds `ci`,`build`,`perf` to reconcile with `.gitmessage` (F158) and is kept in lockstep by a test (F160); the two lists are functionally identical members, py just inherits them implicitly, ts spells them out to have something to test against | A | none — pick one canonical type-enum list; only decide explicit-list-plus-test vs inherited-default if Rust also ports a `.gitmessage`/lockstep test (out of this item's scope) | yes |
| R38 | F156 | `commitlint.dependabot.config.mjs:16` — separate config disables `body-max-line-length`/`footer-max-line-length` for dependabot, selected by PR-author login in CI | absent | area note: "Selected in CI by PR-author login, not applicable without ts's equivalent CI job" — ts simply has no CI job that would consume a per-author config | D | Contingent on F157 (CI re-run of commit-msg linting); if Rust doesn't re-run commit linting in CI, this has nothing to attach to | yes |
| R38 | F157 | `.github/workflows/commitlint.yml:45` — `wagoid/commitlint-github-action@v6.2.1` on every push/PR | absent | inferred: no recorded reason; area note only observes "ts enforces Conventional Commits only via the local `commit-msg` hook; `--no-verify` bypasses it with nothing in CI to catch it" — reads as a gap, not a deliberate choice | D | Does the chosen linter (F149) have a GitHub Action, or would CI re-run need to shell out to the same binary the hook uses | yes |
| R39 secret-scanning-hooks | item | `lefthook.yml:73` — `gitleaks` job runs `scripts/check-gitleaks.sh --staged`; `lefthook.yml:156` `gitleaks-range` job runs `--range` at pre-push; `.gitleaks.toml:10` `[allowlist]`; `.gitleaksignore:1` empty fingerprint file | absent — "ts has no secret-scanning tool at any git-hook tier" | inferred: no recorded reason (no `D-0nn` cites this area); gitleaks is a standalone, language-agnostic Go binary, so its absence in ts is not explained by TypeScript-specific mechanics | D | Adopt gitleaks (already language-agnostic, needs no Rust-specific substitute) at both tiers, or drop; if adopted, port the allowlist/fingerprint-file pattern | yes |
| R40 auxiliary-hygiene-hooks | item | `lefthook.yml:88` `yamllint -c .yamllint`; `lefthook.yml:102` `codespell --toml pyproject.toml`; `lefthook.yml:99` `actionlint {staged_files}`; `.editorconfig:3` `root = true` w/ per-filetype excerpts at `:25`,`:31` | absent for all four (`ts's Justfile:83` only prints an actionlint install hint, never wired) | inferred: no recorded reason for yamllint/codespell/.editorconfig; actionlint is at least name-checked (install hint) but never wired | D | For each of the 4 tools: does the value justify adoption for Rust, independent of language | partly |
| R40 | F167 | `lefthook.yml:82` — `editorconfig-checker` job runs `uv run ec -config .editorconfig-checker.json` on staged files (all file types) | absent — "ts folds trailing-whitespace/EOF-newline fixing into the oxfmt formatter job instead of a separate validity linter (see F084)" | ts's recorded reason: oxfmt already fixes whitespace/EOL for the file types it covers, so the validity linter was folded away, not just omitted | C | Does rustfmt plus the chosen non-Rust-file formatter already cover trailing-whitespace/EOF-newline for every staged file type, or does a gap remain | no |
| R41 lockfile-freshness-check | item | `lefthook.yml:131` — `uv-lock-check` job runs `uv lock --check` when `pyproject.toml`/`uv.lock` are staged | absent | inferred: no recorded reason; plausibly `uv lock --check` is uniquely cheap while pnpm's nearest equivalent (`--frozen-lockfile`) is install-shaped and heavier | E | Is `cargo build/check --locked` a faithful drop-in for `uv lock --check`, and does its cost push it to pre-push instead | no |
| R42 dev-toolchain-provisioning | item | Dedicated installer recipes (`Justfile:159,476,482,713` — install-taplo/install-gitleaks/install-actionlint/install-yamlfmt) plus two synced provisioner manifests (`mise.toml:23`, `.flox/env/manifest.toml:14`, sync note `mise.toml:1`). | None — `Justfile:83` treats actionlint as advisory-only inside check-deps; no mise/flox manifest exists. | D-014(6)/(7): Oxfmt already formats TOML/YAML so taplo/yamlfmt installers are unneeded; broader provisioner omission has no recorded `D-0nn` — inferred: ts's mostly-Node footprint is narrower than py's cross-language (python+cli-tool) mix. | E | Does `cargo`/`rustup` (+ `cargo-binstall`) cover this template's toolchain without a provisioner, or does a declarative manifest (mise/flox) still pay off for the non-cargo tools R39/R40 pick? | no |
| R42 | F183 | `Justfile:159,476,482,713` — install-taplo/install-gitleaks/install-actionlint/install-yamlfmt, latter two shelling to `scripts/install-gitleaks.sh`/`scripts/install-actionlint.sh`. | None — `Justfile:83` folds taplo/yamlfmt into Oxfmt, actionlint is advisory-only. | D-014(6): taplo's npm CLI is stale and Oxfmt formats TOML anyway; D-014(7): Oxfmt formats YAML too — consolidating into one formatter removed the dedicated installer/script layer for those two tools specifically. | C | None on the mechanic — a per-tool installer recipe is only needed for a tool NOT subsumed by whatever linter/formatter R27–R29 picks; gitleaks/actionlint provisioning itself is R39/R40's call. | no |
| R43 ai-assistant-repo-furniture | item | None — dropped `.windsurfrules`/Cursor rules pre-pin (commit `fc8d944`), consolidated into the AGENTS.md hub only (F197); no `companyAnnouncements` key in `.claude/settings.json`. | `.cursor/rules/projectenv.mdc:9` — "read rules @AGENTS.md"; `.windsurf/rules/justfile-rules.md:2` — glob rule on `Justfile`; `.claude/settings.json:2` — `companyAnnouncements` array. | F198 per D-024(5): Windsurf demoted `.windsurfrules` to legacy, both vendors read AGENTS.md natively. F199 per D-024(6): reused the org's difftree `companyAnnouncements` convention per the owner's own global instruction mandating repo-welcome announcements. | F (F199: D) | See sub-rows — the two facts have different Rust questions. | yes |
| R43 | F198 | None — dropped `.windsurfrules`/Cursor rules in commit `fc8d944` (pre-pin), consolidated to AGENTS.md only. | `.cursor/rules/projectenv.mdc:9`; `.windsurf/rules/justfile-rules.md:2` — thin spokes re-pointing to the AGENTS.md hub. | D-024(5): Windsurf demoted `.windsurfrules` to legacy and reads AGENTS.md natively; Cursor supports AGENTS.md too — ts kept the spokes rather than dropping them. | F | Adopt vendor spoke files for Cursor/Windsurf in rs, or rely on the AGENTS.md hub alone — both viable per current vendor docs; owner call, applied once for the org. | yes |
| R43 | F199 | None — no `companyAnnouncements` key in `.claude/settings.json`. | `.claude/settings.json:2` — `companyAnnouncements` array with project summary + `just` command list. | D-024(6): reused the org's difftree `companyAnnouncements` convention per the owner's own global instruction mandating repo-welcome announcements for repos they set up. | D | None — inherit the harmonized convention: rs's `.claude/settings.json` gets a `companyAnnouncements` string naming the project and its `just` recipes (see F176, `COMMON → REUSE`). | yes |
| R44 devcontainer-environment | item (F200) | `.devcontainer/devcontainer.json:4` — pinned `ghcr.io/astral-sh/uv:python3.12-bookworm-slim`; `.devcontainer/post-create.sh:15` — runs `make bootstrap` then `just setup`. | None — no `.devcontainer/` directory; no `D-0nn` explains the omission. | No recorded reason. `inferred:` ts's Node toolchain needs no isolated container (npm/nvm is lightweight, widely pre-installed), so the extra maintenance surface wasn't judged worthwhile. | D | Whether-to-adopt is language-neutral; if adopted, does a maintained Rust base image/devcontainer feature exist, and does its `postCreate` just reuse F179–F182's bootstrap chain? | partly — adopt-or-drop is a shared org call (yes); the base image and in-container config, if adopted, is Rust-specific (no) |
| R45 pr-comment-bot-trigger-block | item (F204) | `.github/pull_request_template.md:90` — "Review Trigger" section with `@coderabbitai review`/`@greptile-apps review`/`@cubic-dev-ai review`. | None — not carried into the ts PR template; relies on bots' auto-review-on-push. | No recorded reason (`TS_PORT_DECISIONS.md` has no entry). `inferred:` auto-review-on-push likely made an explicit manual re-trigger doc feel redundant to the ts port. | D | None on whether-to-include — adopt or drop as one org-wide PR-template convention; which bots and their current `@`-mention syntax is R18's call, not this item's. | yes |
| R46 per-file-license-header | item (F207) | `src/py_launch_blueprint/__init__.py:1` — full 18-line MIT header comment, present in 51 files. | `.github/dependabot.yml:1` — legacy unconverted 18-line header in 12 config/workflow files; `.github/FUNDING.yml:1` — the one file where the SPDX conversion actually landed. | D-024(7) intended replacing the 18-line header with a one-line SPDX comment everywhere; in the pinned commit that conversion landed only in `FUNDING.yml` — an incomplete execution of a decision, not a deliberate divergence. | D | None on the target shape — D-024(7)'s intent (one-line SPDX header, no insertion automation) is already the org's decided answer; open question is only the correct dual `MIT OR Apache-2.0` SPDX syntax and doc-comment placement. | yes |
| R47 contributors-recipe-mode | item (F210) | `Justfile:462` — `update-contributors:` runs `npx contributors-please@1 init --non-interactive ...`. | `Justfile:244` — `@contributors:` runs `pnpm dlx contributors-please render`. | D-024(9): py's local `init` recipe is called an "orphan" diverging from the CI action's own behavior; ts's `render` recipe reproduces the same ledger-to-Markdown step the shared `contributors-please-action` performs. | A | None on subcommand choice — inherit `render` per D-024(9)'s reasoning. Separately: `contributors-please` has no Rust-native equivalent, so rs's recipe shells out via a Node package runner regardless. | yes |
| R48 mocking-crates | item | `unittest.mock` for spies/doubles; `responses` intercepts real HTTP calls | `vi.fn`/`vi.mock` for spies; plain `fetchImpl` fake, no interception library | F119 is a same-pattern tool swap; F120 is architectural — ts needs no HTTP-mock library because transport is DI-injected | mixed — see sub-rows | see sub-rows; F120 is coupled to R01's architecture call | partly |
| R48 | F119 | `tests/cli/test_pylb.py:6` — stdlib `unittest.mock` (`Mock`, `patch`) | `tests/cli.test.ts:12` — Vitest built-in `vi.fn`/`vi.mock` | D-019(5): same DI-and-spy pattern, only the framework-native tool differs | B | does Rust need `mockall` for trait-object doubles, or do hand-written fakes/closures suffice (per R32/R48 prompt's own framing) | no |
| R48 | F120 | `tests/core/test_py_api_repository.py:104` — `@responses.activate` intercepts real HTTP calls | `tests/api.test.ts:27` — `fetchImpl` passed as a plain fake, no interception library at all | D-019(5): DI preserves hermetic-boundary intent with compile-time seam safety, "structurally preventing" a wrong-seam bug py's global-patch approach had | F | if R01 adopts DI/ports (as ts did), no HTTP-mock crate is needed at all; if not, pick `wiremock` or `mockito` — decided together with R01, not independently | no |
| R49 build-target-declaration | item | `pyproject.toml:133,139,141` — `build-backend = "uv_build"`, one implicit build unit, wheel content bounded by `module-name`/`module-root` | `tsdown.config.ts:8-10` — bundler config, `entry: ['src/cli.ts','src/lib.ts']`; `package.json:18,20,22` — bin path + `files:["dist"]` whitelist | D-012(1)/(3) (adjacent — explains ts's own picks, not the gap): py/ts each need an external tool since neither language has a native build system; inferred: Cargo is that native system for Rust | C | Do `[[bin]]`/`[lib]` targets plus default `include`/`exclude` fully cover what py's/ts's tools each needed a separate package for? | no |
| R50 install-smoke-test | item | `.github/workflows/ci.yml:204` — `build-smoke` job on every code-touching PR | `Justfile:276` — `pack-check` recipe exists (per D-012(7)) but is never invoked from `ci.yml` | no D-0nn cited; inferred: circumstantial — py's cheap `uvx` one-liner made CI-gating easy, ts's multi-step recipe stayed local-only | A | CI-wire the smoke test like py? (Which mechanism it runs is the F222 sub-row.) | partly |
| R50 | F221 | `.github/workflows/ci.yml:204` — CI-gated, every PR | `Justfile:276` — recipe exists, local-only, not in `ci.yml` | inferred: no stated reason ts left it local-only beyond the recipe's own D-012(7) scope (packaging validation) not extending to CI wiring | A | Should the Rust smoke test run on every code-touching PR (py's cadence), given Rust's longer compile times may make that costlier than py's install? | yes |
| R50 | F222 | `.github/workflows/ci.yml:222` — `uvx` ephemeral-env install-and-run of the just-built wheel | absent — ts's `pack-check` validates the tarball's contents/exports but has no distinct "install into a throwaway env, run it" step captured as its own fact | inferred: `uvx` is a Python-specific one-line ephemeral-installer tool; the JS ecosystem has no equally trivial equivalent, so ts folded partial coverage into `pack-check` instead | B | What is the Rust-native ephemeral-install analogue — `cargo install --path . --root <tmp>` then run the binary, or `cargo run --locked`? | no |
| R51 container-image | item | Non-root multi-stage image ships as the web extra's distribution artifact: `Dockerfile:13` (`uv sync --frozen --no-install-project --no-dev --extra web`), `Dockerfile:21` (`USER app`), `Dockerfile:29` (`HEALTHCHECK` probing `/healthz` via `urllib`, no `curl` in the slim base). | absent — no Dockerfile, no container-related file (`docs/port/areas/web-service.md` F303 note: no web/HTTP-server code anywhere in ts). | inferred: no reason recorded for the image itself; downstream of R69 — ts never built the web service the image would package. F017 (workspace-architecture, `ADOPT`, no research item) already committed rs to an optional web Cargo feature mirroring py, not ts — an owner call already exercised, not proposed here. | B | Given R69 resolves a framework, what multi-stage non-root Dockerfile pattern (cargo-chef or plain COPY-order caching) and healthcheck mechanism (curl-less minimal runtime) fits — contingent on R69's `web-extra-surface`. | no |
| R52 toml-crate | item | stdlib `tomllib` parses (`core/config.py:40`); separate `tomli_w` writes (`core/config.py:45`) | `smol-toml`'s `parse`/`stringify` do both directions (`src/lib/config.ts:20`), D-017(2) | inferred: py split parse/write because stdlib ships only a reader; ts had one current, zero-dep npm package (D-017(2)) covering both | B | Which crate(s) (`toml`, `basic-toml`, `toml_edit`) give parse+write with serde integration and py's atomic 0600 write — one crate like ts or split like py? Per R52 prompt. | no |
| R53 config-schema-validation | item | pydantic `BaseModel`/`ValidationError` (`core/settings.py:33`) | zod (`src/lib/config.ts:21`), D-017(4) | language-bound validation library choice; D-017(4): ts kept the org's Zod precedent, upgraded v3→v4 for size/speed | B | Does `serde` derive alone (`deny_unknown_fields`, enum fields) reach pydantic/zod's field-level + aggregate-error parity, or is a validation crate (validator/garde) needed? Per R53 prompt. | no |
| R54 config-discovery-tiers | item | layered system→user→project, later wins (`core/config.py:134`); system dirs incl. Windows `%PROGRAMDATA%` (`core/paths.py:108`); project-local upward search (`core/paths.py:184`); `--config` env alias `PLBP_CONFIG` (`cli/options.py:107`) | absent — single default-user-path tier only | D-017(1)'s own target precedence (flags>env>project>user>system>defaults, cli-standards R5.1) matches py; ts shipped only the user tier — the gap itself is not explained beyond scope; inferred: deferred, not deliberate | D | None — cli-standards R5.1 already normalizes the full chain; adopt py's layered discovery + env alias, the standard ts's own decision cites but didn't fully build. | yes |
| R55 config-error-tolerance | item | missing `--config` path tolerated, empty layer (`core/config.py:114`); unparsable discovered layer → warning (`core/config.py:102`); invalid value → warning, key dropped (`core/settings.py:169`) | missing `--config` path throws `UsageError`, exit 2 (`src/lib/config.ts:153`); no discovered-tolerant tier; invalid value → whole-file `ConfigError` (`src/lib/config.ts:129`) | three distinct causes — see sub-rows | F/D/C (mixed, see sub-rows) | See sub-rows. | partly |
| R55 | F235 | missing explicit path tolerated as empty layer, valid `config set` target (`core/config.py:114`) | throws `UsageError`, exit 2 (`src/lib/config.ts:153`) | ts's code comment cites `click.Path(exists=True)` parity, but py's pinned `--config` option (`cli/options.py:105`, `dir_okay=False`) does not set `exists=True` — area notes flag the mismatch | F | Does Rust want py's tolerant "missing path = empty layer" (usable as a `config set` target) or ts's strict fail — noting ts's stated reason may rest on a false premise about py? | no |
| R55 | F236 | unparsable discovered layer degrades to warning, not crash (`core/config.py:102`) | absent — ts's one discovered file is read the same strict way as `--config`, no tolerant discovered tier | contingent on R54: ts never built a discovered tier at all, so no tolerant-vs-strict choice existed to make | D | Only relevant if R54 adopts a discovered tier; if so, inherit py's warn-and-skip. | yes (contingent on R54) |
| R55 | F238 | invalid config *value* drops just that key with a warning (`core/settings.py:169`) | absent — zod `safeParse` failure raises `ConfigError` for the whole file (`src/lib/config.ts:129`) | inferred: pydantic's per-field validation naturally supports drop-and-continue; whole-struct typed deserialization (zod, likely serde) makes partial-drop awkward — ledger flags this as a considered-and-rejected OVERRIDE candidate | C | Can `serde` realistically drop one bad key and keep the rest, or does it force parsing into an untyped `toml::Value` first? Per R55 prompt. | no |
| R56 config-secret-policy | item | token never read from config file, flag>env only, two tiers (`core/config.py:169`); ADR-0002 rationale | config file is lowest-precedence token source, flag>env>file, three tiers (`src/lib/config.ts:171`, `:177`) | py's ADR-0002 is a deliberate security decision (never persist a bearer token to disk); ts's D-017(1) instead followed cli-standards R5.1's generic flag>env>file chain with no security override | F | Does Rust adopt py's no-token-in-file security stance or ts's file-as-source convenience — a security-policy call, not a language-mechanics one. | partly (see F245 sub-row) |
| R56 | F245 | absent — no file token, so no permission-warning concept exists | warns when config file mode `& 0o077 != 0` (`src/lib/config.ts:135`) | only meaningful because ts's config file can carry a token (F239); py never checks discovered-file permissions since it never stores a secret there | D | Moot unless R56 adopts ts's file-as-token-source; if adopted, port the loose-permissions warning too. | yes (contingent on R56) |
| R57 xdg-directory-set | item | separate `data_home()`/`state_home()`/`cache_home()` accessors beyond config (`core/paths.py:130`) | absent — ts resolves only a config dir (`src/lib/xdg-paths.ts:21`); no data/state/cache dirs because there is no log file or local db to place in them | ts had no feature needing those dirs (no file log sink, no crash log); not a language limitation | D | Contingent on R59 (file-log sink) and R67 (crash log) — if either needs a state path, adopt the full XDG set; else config-only like ts. | yes (contingent) |
| R58 logging-pipeline-architecture | item | TTY-aware JSON/text auto-select (`core/logging.py:120`); structlog processor chain (`core/logging.py:154`); key-based secret redaction (`core/logging.py:93`); optional OTel span correlation (`core/logging.py:106`); explicit `--log-level`/`PLBP_LOG_LEVEL` (`cli/options.py:85`); idempotent owned-handler teardown (`core/logging.py:226`); one pipeline as two front-end profiles (`web/logging.py:20`) | absent — hand-rolled leveled stderr-writer logger only (`src/lib/logger.ts:47`, `:58`); no structured/JSON mode, no redaction, no OTel, no `--log-level` (only relative `-v`/`-q`/`--debug`), no reconfig-teardown logic, single CLI front-end | D-018(1): org-unanimous Synthesis #6 default (agent2linear/claim-npm precedent) chose a minimal injected-writer logger, deliberately simpler than py's rich pipeline — not a JS capability gap (winston/pino exist) | F | Does Rust follow py's `tracing`+structured-pipeline richness or ts's hand-rolled minimalism? The prompt is framed as a crate search, but the real question is scope, not tooling. | no (owner call; F263 also contingent on R69 web surface) |
| R59 file-log-sink | item | `[logging]` config table (`core/settings.py:49`); rotating file sink 10 MiB × 5 backups (`core/logging.py:241`, `:82`); `--log-file`/`PLBP_LOG_FILE` flag+env (`cli/options.py:93`); flag/env>config>off precedence (`cli/context.py:212`); env format override (`cli/context.py:233`); dual-sink level floor (`core/logging.py:259`) | absent — no file sink at all; config schema carries no logging section | direct consequence of R58's minimalism call (D-018(1)); ts's logger writes only to the injected stderr writer, so no file-sink feature tree could exist | F | Ships only if R58 adopts py's richer pipeline; if so, does the chosen crate (e.g. `tracing-appender`) support size+backup-count rotation (py's shape) vs the more common time-based default? Per R59 prompt. | no (contingent on R58) |
| R60 cli-parsing-framework | item | click (`cli/main.py:29`); noun-verb groups (`commands/__init__.py:26`); extended `--version` (`main.py:52`); completion cmd (`main.py:89`); blanket env binding (`main.py:66`); global opts stack anywhere (`options.py:232`) | Commander (`router.ts:13`); default cmd + flat config (`commands/projects.ts:70`); plain `--version`; no completion cmd; no env binding; opts declared once on root (`router.ts:125`) | D-016(1): Commander picked as org-precedented, cli-standards-sanctioned framework over oclif/parseArgs; ts also ported an older flat py CLI predating its noun-verb refactor, dropping several click-native features it never carried over | B | Pick a Rust parsing crate (clap likely) and re-derive noun-verb shape, `--version` extras, completion, env binding, option stacking from its own feature set — see F265/F267 sub-rows | no |
| R60 | F265 | noun-verb groups `commands/__init__.py:26` | default-cmd + flat config `router.ts:70` | inferred: ts ported an older, pre-refactor flat py CLI shape rather than the current noun-verb design — a stale-port gap, not a language constraint | F | Adopt current py's noun-verb shape (the maintained design), not ts's stale flat shape — decide once, apply to all three going forward | yes |
| R60 | F267 | extended version (tool/python/platform) `main.py:52` | absent (version string only) | no recorded reason; ts's `--version` never carried this extra printing — a scope gap in the ledger, not a stated tradeoff | D | Print target-triple/rustc info in `--version`? Needs a build-info crate (rustc_version/built/vergen), independent of F264's framework pick | yes |
| R61 interactive-prompts | item | confirm `options.py:278` (`click.confirm`); guided value `commands/config.py:94` (`click.prompt`) | multi-select checkbox `commands/projects.ts:157`/`adapters.ts:41` (`@inquirer/prompts`, D-016(6)) | inferred: each repo ships prompts only where its own command set needs them — py's `config init` needs guided-value/confirm, ts's `projects` fetch needs a checkbox; no command overlap requiring both | D | Does rs's own command set need confirm/guided-value/multi-select, and does one crate (dialoguer/inquire) cover all three shapes? | partly |
| R62 clipboard-integration | item | absent (no `path:line` in area evidence) | `--copy` flag `commands/projects.ts:84`; headless-degrade write `adapters.ts:49`, re-raised as `CliError` `commands/projects.ts:193` | no recorded reason; D-016(7) frames this as a "pyperclip replacement," implying py precedent, but no py `path:line` is cited anywhere — see Notes | D | Does rs need `--copy`? `arboard` is the plausible cross-platform crate; must degrade cleanly on headless Linux like ts does | yes |
| R63 progress-spinner | item | absent | spinner `commands/projects.ts:129`; gated on stderr TTY + absence of `CI` (D-016(5)) | no recorded reason for py's absence; D-016(5) frames this as a "rich.Progress replacement," implying py precedent not reflected in the area ledger — see Notes | D | Does rs need a fetch-progress spinner? `indicatif` is the plausible crate; must default to stderr and respect the CI/TTY gate R65 defines | yes |
| R64 pager-integration | item | pager for text output `output.py:177`; precedence `PLBP_PAGER`>`PAGER`>`less -FRX` fallback `output.py:88,56`; ADR 0008 | absent | no recorded reason; prompt notes no ts decision record even considered paging — could be oversight or a deliberate scope call | D | Is paging worth adopting given rs's typically short output? If yes: subprocess-pipe preserving ANSI color, plus the pager env-var precedence | partly |
| R65 color-enablement-chain | item | chain flag>`NO_COLOR`>config `context.py:188`; gated once, reused for both streams `output.py:144` | chain flag>`NO_COLOR`>`FORCE_COLOR`>TTY, no config layer `colors.ts:26`; per-stream (stderr only) `router.ts:100` | D-018(2)/D-038: ts adopted `FORCE_COLOR` + TTY-only gating (no config tier) via styleText/picocolors; py's config system predates any FORCE_COLOR convention and ts never added a color config key | A | Pick one canonical chain (flag > NO_COLOR > FORCE_COLOR > config > TTY) for all three; does an ecosystem crate (anstream/supports-color) implement it directly? | yes |
| R65 | F290 | stream's own `isatty()`, separate from rich's `is_terminal` `output.py:94` | injectable `stdoutIsTTY`/`stderrIsTTY` on `CliDeps` `router.ts:65` | inferred: each repo's TTY check follows its own language's idiomatic stream-introspection API and test seam — not a shared setting | B | What Rust crate/pattern (e.g. `is-terminal`) gives an equivalently mockable TTY seam for tests? | no |
| R66 output-format-surface | item | formats text/json/markdown `output.py:47`; `-o`/`--output` selects format, separate `--output-file` redirects `options.py:47,66,72` | formats text/json/csv `format.ts:16`; `--format` selects, `--output <file>` redirects `commands/projects.ts:82-83` (D-033) | no D-0nn covers the 3rd-format pick; naming collision resolved by D-033 — ts kept `--output` as file sink, renamed py's format flag to `--format` | F | Pick markdown, csv, both, or neither as the 3rd format; settle flag names (favor ts's already-adjudicated `--format`/`--output` split) once for all three | partly |
| R66 | F291 | markdown `output.py:47` | csv `format.ts:16` | no recorded reason; each repo picked a different 3rd format for a different use case (markdown=human/PR-paste vs csv=spreadsheet import) — inferred | F | Markdown, csv, both, or neither — owner call on use case; may need a `csv` crate or hand-rolled markdown-table writer | partly |
| R66 | F293 | `-o`/`--output` selects format, `--output-file` redirects `options.py:66,72` | `--format` selects, `--output <file>` redirects `commands/projects.ts:82-83` | D-033: ts deliberately kept `--output` as the file sink (source parity) instead of an external `-o`-as-format convention, resolving the collision by renaming py's format flag to `--format` | A | none — inherit ts's already-adjudicated split (`--format` selects, `--output` redirects) | yes |
| R67 error-and-exit-code-contract | item | stable catalog `core/errors.py:51`; codes 0-5 `core/errors.py:34`; SIGINT-only→5 `options.py:215`; `hint` field `core/errors.py:76`; crash log `options.py:143` (ADR 0006); no EPIPE/prompt-cancel handling | `err.name` code `router.ts:211`; cli-standards codes incl. SIGTERM `lib/errors.ts:13`,`cli.ts:21`; inline remedy text, no hint field; EPIPE handling `cli.ts:17`; prompt-cancel→130 (D-033); no crash log | D-016(2): ts reconciled py's ad hoc codes onto org-normative cli-standards R6.1; D-033 added prompt-cancel/EPIPE as gaps the S3b gate found; hint field and crash log have no recorded D-0nn | F | What error-code catalog, exit-code taxonomy, and signal/EPIPE/crash-log conventions does rs adopt for the shared `error-taxonomy-exit-codes` contract? See sub-rows | partly |
| R67 | F295 | stable catalog e.g. `PLBP000` `core/errors.py:51` | `err.name` (exception class name) `router.ts:211` | no recorded D-0nn for this field; inferred: ts's mechanism reuses JS's built-in `Error.name` reflection rather than a deliberately designed catalog | C | A Rust error enum (thiserror/strum) can give an exhaustive stable-string catalog like py's — py's deliberate catalog or ts's reflection shortcut (Rust has no free `err.name` equivalent)? | no |
| R67 | F296 | `hint` field, rendered separately `core/errors.py:76` | inline remedy text in the message (e.g. `missingTokenMessage`), no hint field | no recorded D-0nn; inferred: ts chose inline simplicity over structural separation, no rationale on record | F | Structured `hint` field or inline remedy text — depends on which output format (R66) renders it; owner call, no crate research needed | partly |
| R67 | F297 | codes 0-5 `core/errors.py:34` | cli-standards 0/1/2/3/4/5/130/143 `lib/errors.ts:13` | D-016(2): ts reconciled py's ad hoc codes onto the org-normative cli-standards R6.1 taxonomy | A | none — inherit the cli-standards taxonomy ts already adopted (D-016(2)) | yes |
| R67 | F298 | SIGINT-only→exit 5 `options.py:215` | SIGINT→130, SIGTERM→143 separately `cli.ts:21` | D-016(2)/D-033: same cli-standards reconciliation as F297; ts additionally handles SIGTERM, which py's non-daemon CLI never needed | A | none — inherit cli-standards 130/143 signal mapping already adopted by ts | yes |
| R67 | F299 | absent (no interactive multi-select to cancel) | prompt-cancel→exit 130 `router.ts:200` (D-033) | D-033: contingent on R61's multi-select prompt existing at all; py has no such prompt to cancel | D | If R61 adopts an interactive-prompt crate, does it surface a typed cancellation error rs can map to exit 130 (matching ts's `ExitPromptError`)? | yes |
| R67 | F300 | absent | EPIPE handling `cli.ts:17` | inferred: Node needs explicit `stdout.on('error', ignoreEpipe)`; Python's default SIGPIPE disposition (or click/rich) likely swallows this without app code — not confirmed in evidence | C | Rust's default SIGPIPE disposition differs from both — does writing to closed stdout need an explicit `ErrorKind::BrokenPipe` check? | no |
| R67 | F302 | crash-log file `options.py:143` (ADR 0006) | absent | no recorded reason for ts's omission; a language-neutral best-effort feature ts simply didn't port | D | Does rs want a best-effort crash log? Path may depend on R57's (xdg-directory-set) state/cache-dir outcome | yes |
| R68 release-binary-artifacts | item | `.github/workflows/ci.yml:216` — `uv build` produces sdist + wheel (two PyPI formats) | `package.json:23` — `"type": "module"`, single ESM npm tarball | inferred: each format-count mirrors what its own registry accepts (PyPI: sdist+wheel; npm: one tarball) — a packaging-mechanic fact, not a design choice | G | Neither repo ships pre-built GitHub-Release binaries. Add `cargo-dist` cross-compiled binaries and `cargo-binstall` metadata, or stay crates.io-only? | no |
| R69 web-framework-stack | item | FastAPI app-factory (`src/py_launch_blueprint/web/app.py:102`, `create_app()`); config DI with lazy-load fallback (`web/deps.py:37`); uvicorn entrypoint with `timeout_graceful_shutdown` (`web/__main__.py:37,42`); `Justfile:291` dev-serve recipe with auto-reload, console log default. | absent — ts README scopes the project as "TypeScript port of py-launch-blueprint" = "CLI + library" only; no web/HTTP-server dependency, code, or test anywhere. | inferred: ts port scoped itself CLI + library from the outset; `TS_PORT_DECISIONS.md` has zero web-service entries — never evaluated, not rejected. F017 already `ADOPT`ed an optional web Cargo feature for rs (mirrors py, diverges from ts) — the existence question is already settled outside this item. | B | Which async framework (axum/actix-web/etc.) fills the already-adopted feature, its config-DI/lazy-fallback idiom, graceful-shutdown entrypoint, and dev-reload Justfile recipe — the `web-extra-surface` value nine other items consume. | partly — scope level only: whether ts-launch-blueprint should also ship the web tier is one org call, flagged here once (R69 owns `web-extra-surface`); the framework pick itself is Rust-only |
| R70 http-problem-envelope | item | Single RFC 9457 envelope for every non-2xx, incl. framework 422s (`web/problems.py:52` `PROBLEM_CONTENT_TYPE`); domain-error→status table (`web/problems.py:57` `ERROR_STATUS: dict[type[PyError], int]`). | absent — no error-handling/response-envelope code (web service absent entirely). | inferred: same root cause as R69 (ts never built the web tier this envelope would sit on); no D-0nn addresses it. | B | Does a Rust crate render RFC 9457 problem+json integrated with the chosen framework's error hook, covering domain + framework + validation errors alike; what idiom gives a status-mapping table analogous to `ERROR_STATUS` (feeds off R67's not-yet-decided error taxonomy)? | no |
| R71 openapi-generation-pipeline | item | OpenAPI post-processed to match the error envelope (`web/problems.py:104`); curated `tag-function` operation ids (`web/app.py:77`); committed snapshot staleness-tested (`docs/api/openapi.json`, `tests/web/test_openapi_snapshot.py:30`); CI breaking-change gate via `oasdiff` (`.github/workflows/api-contract.yml:44`). | absent — no OpenAPI generation, snapshot, or CI contract-diff job anywhere in ts. | inferred: same root cause as R69 — no web/API surface in ts to generate a schema from. | B | Which OpenAPI-generation crate (`utoipa`/`aide`/`okapi`) supports post-generation rewriting and stable operation-id overrides; what mirrors the committed-snapshot staleness test; does a Rust-ecosystem `oasdiff` equivalent exist for CI breaking-change detection or must `oasdiff` itself be invoked against a Rust-generated spec? | no |
| R72 api-pagination | item | Page/size query params, items/total envelope via `fastapi-pagination` (`web/routers/projects.py:30`); wired app-wide via one factory call (`web/app.py:204` `add_pagination(app)`). | absent — no pagination code or dependency in ts. | inferred: same root cause as R69. | B | Does a Rust crate give `fastapi-pagination`-equivalent page/size extraction plus an items/total envelope, registrable once app-wide (vs. per-route under Rust's typed-extractor model), keeping collection items as the caller's own domain type? | no |
| R73 idempotency-middleware | item | `Idempotency-Key` replay on POST/PUT/PATCH (`web/idempotency.py:43`); only 2xx outcomes cached (`web/idempotency.py:141`); in-memory `OrderedDict` store, documented Redis-swap point (`web/idempotency.py:91`). | absent — no idempotency code in ts. | inferred: same root cause as R69. | B | Does a published Rust middleware crate already implement idempotency-key replay for the chosen framework, or is it hand-rolled over a generic key-value trait; what idiom expresses "2xx-only cache write" and mirrors py's cache-key scoping? | no |
| R74 rate-limiting-middleware | item | In-memory single-instance limiter, same documented Redis-swap point as idempotency (`web/app.py:220` `Limiter(` with no `storage_uri`); off by default, one env var enables it (`web/settings.py:70` `rate_limit: str \| None = None`). | absent — no rate-limiting code in ts. | inferred: same root cause as R69. | B | Which Rust rate-limiting crate (e.g. a `tower`-compatible governor-based layer) is idiomatic, defaults to (or documents a swap point to) an in-memory backend, is off-by-default/env-gated, and produces an accurate `Retry-After`? | no |
| R75 http-middleware-stack | item | Contractual outermost-first ordering, id/security-header middleware outermost (`web/app.py:145`); request-id propagation (`web/middleware.py:72,82`); one canonical structured access-log event (`web/middleware.py:95`), excluding probe paths (`web/middleware.py:49` `ACCESS_LOG_EXCLUDED_PATHS`); unconditional security headers (`web/middleware.py:56` `SECURITY_HEADERS`); uvicorn's own loggers folded into the shared pipeline (`web/logging.py:71`). | absent — no middleware/logging code in ts. | inferred: same root cause as R69. | B | Is a layered `tower`-`Service`-composition model the idiomatic analogue of `add_middleware`; what ordering keeps id/security-header layers outermost across R73/R74/R76's layers; how is the framework/server's own access log silenced into the shared `tracing` pipeline (depends on R69's framework pick)? | no |
| R76 cors-middleware | item | Installs CORS middleware only when a non-empty origin allowlist is configured; `src/py_launch_blueprint/web/app.py:136` — `if settings.cors_origins:`; empty list (default) = no CORS middleware at all. | absent — no web/HTTP-server code exists anywhere in the ts repo (`docs/port/areas/web-service.md:7`, F303). | inferred: ts never built a web service at all (F303, py-only), so there was nothing to add CORS middleware to; `TS_PORT_DECISIONS.md` records no entry addressing web-service scope. | B | Which Rust CORS crate (e.g. tower-http `CorsLayer`) installs conditionally on a runtime, possibly-empty allowlist, matching py's "empty = no middleware" semantics? | no |
| R77 web-env-settings | item | Typed env settings loaded under an app-derived prefix; `src/py_launch_blueprint/web/settings.py:38` — `ENV_PREFIX: str = f"{APP_NAME.upper()}_WEB_"` (pydantic-settings). | absent — no web settings object exists; ts has no web service (F303). | inferred: same root cause as R76 — ts never built a web service, so no web-only settings object exists to compare; no recorded ts decision. | B | Which crate loads typed env-var settings with a derivable prefix and threads them through the chosen framework's state/DI (e.g. `envy`/`figment`/hand-rolled `serde`+`std::env`)? | no |
| R78 opentelemetry-integration | item | OTel tracing is opt-in and soft-imported at runtime; `src/py_launch_blueprint/web/telemetry.py:71` — `except ModuleNotFoundError:` degrades to a warning, not a crash, when the `otel` extra is absent. | absent — no web service, so no tracing integration; ts has no `otel`-equivalent extra (F303). | no recorded reason; inferred: root cause is F303 (ts has no web service) — py's runtime soft-degrade has no ts analog to compare. | B | What OTel SDK/exporter + `tracing-opentelemetry` bridge crates go behind an `otel` Cargo feature, and what runtime fallback (feature compiled in, no collector configured) replaces py's soft-import degrade? | no |
| R79 prometheus-metrics | item | RED metrics exposed at `/metrics`, on by default, no env gate; `src/py_launch_blueprint/web/telemetry.py:48` — `.expose(app, endpoint="/metrics", include_in_schema=False)`; excluded from its own measurements and the OpenAPI schema. | absent — no web service, no `/metrics` route (F303). | inferred: root cause is F303 (ts never built a web service); no recorded ts decision to weigh against. | B | Which crate/combination (e.g. `axum-prometheus`, or `metrics` + `metrics-exporter-prometheus`) provides on-by-default RED metrics that self-exclude from measurement and from the generated OpenAPI schema? | no |
| R80 health-probe-endpoints | item | Liveness `/healthz` reports version+runtime info (`src/py_launch_blueprint/web/app.py:153`); readiness `/readyz` reruns the same diagnostics as the CLI `doctor` command, 503 problem doc on failure, reusing `core/diagnostics.py`'s `run_diagnostics` as one source of truth (`src/py_launch_blueprint/web/app.py:184`). | absent — no `/healthz`/`/readyz` routes, no web service to host them (F303). | no recorded reason; inferred: root cause is F303 — the CLI/web diagnostics-sharing pattern (F329) has no ts counterpart since ts's CLI has no web layer to share with. | B | What crate (if any — `built`/`vergen`/`env!("CARGO_PKG_VERSION")`) supplies liveness version/build info, and what shared Rust type/function do a CLI `doctor` command and `/readyz` both call as one diagnostics source? | no |
| R81 docs-delivery-model | item | generated Sphinx+MyST+Furo HTML site hosted on Read the Docs, MyST markdown, `{toctree}` navigation, separate landing page, `sphinx-autobuild` preview, `sphinx-quickstart` scaffold; `.readthedocs.yaml:44`, `docs/source/index.md:1` | README-centric front door + plain CommonMark/GFM `docs/` tree rendered by GitHub, no generator/host, bullet-list navigation, merged landing content, no preview/scaffold tooling; `docs/docs.md:1`, `README.md:137` | D-023(1): org practice is unanimously README-centric (5/5 recent repos) and the TS docs-generator field is at an awkward maturity moment; the template's value is the Diátaxis content, not the Sphinx machinery | F | is `mdBook` — Rust's own strong ecosystem precedent (unlike ts's weak generator options) — reason enough to diverge from the org's README-centric default, or does that default still win for a template; F343/F344/F345/F346/F351/F352 (dialect, nav, duplication, pointer, preview, scaffold) are pure consequences of whichever way this goes | yes |
| R82 docs-correctness-gate | item | `sphinx-build -W` fails CI on any broken cross-reference/autodoc error (`.github/workflows/ci.yml:242`); weekly `sphinx-build -b linkcheck` external-URL job (`dep-audit.yml:69`); `autodoc` wired, zero directives used | `just docs-check` runs `scripts/check-links.mjs`, a dependency-free Node script checking only internal relative links (`.github/workflows/ci.yml:98`, `scripts/check-links.mjs:1`); no external-URL check; TypeDoc optional, not CI-gated | D-023(5): py's gate also covers autodoc/directive errors, ts's is link-resolution only; `check-links.mjs` chosen over lychee/markdown-link-check per the script's own header (not installed on this machine/CI image); D-023(4): API-doc generator matched at equal (unused) investment in both | B | is `lychee` the dominant Rust-ecosystem link checker (internal+external in one tool, per the prompt's own HIGH question), and does `cargo doc` with `RUSTDOCFLAGS="-D warnings"` give py's autodoc-error strictness for free — coupled to R81's delivery-model pick for which surface it walks | no |
| R82 | F350 | weekly `sphinx-build -b linkcheck` job; `.github/workflows/dep-audit.yml:69` | absent — no external-URL check | no D-023 entry addresses external-URL checking specifically; area notes confirm D-023(5) only speaks to the internal-link replacement — inferred: silently dropped, not a deliberate omission | D | adopt a periodic external-link check in all three repos, or drop it everywhere — what does current Rust tooling (e.g. lychee) recommend for cadence/flakiness handling | yes |
| R83 openapi-contract-fuzzing | item | Fuzzes every documented OpenAPI operation via `schemathesis`, asserting no server error, marked `slow` and skipped by default; `tests/web/test_contract.py:30` — `case.call_and_validate(checks=(not_a_server_error,))`. | absent — no web service and no OpenAPI schema to fuzz against (F303). | no recorded reason; inferred: root cause is F303; split from R71 at Task 10 as its own tool-ecosystem survey (`docs/port/COMMONALITY.md:338`). | B | Does a Rust crate perform OpenAPI-schema-driven generative fuzzing against a live service (dedicated crate, or `proptest`/`quickcheck` + a schema-to-strategy crate), matching `schemathesis`'s zero-hand-written-data behavior? | no |
| R84 openapi-typed-client-generation | item | Generates a typed API client from the committed OpenAPI snapshot via a Justfile recipe, never hand-written; `Justfile:312` — `uvx openapi-python-client generate --path docs/api/openapi.json ...`. | absent — no web service and no OpenAPI schema to generate a client from (F303). | no recorded reason; inferred: root cause is F303; split from R71 at Task 10 as its own tool-ecosystem survey (`docs/port/COMMONALITY.md:339`). | B | Does a Rust codegen tool (`progenitor`, `openapi-generator`'s Rust target, or `openapiv3`-driven hand-rolled codegen) produce an idiomatic, drop-in typed client from the committed snapshot? | no |
| R85 rich-terminal-row-niceties | item | `table_rows_rich()` hook `core/models.py:59`; `relative_time()` `core/format.py:46`; `rich_link()` OSC-8 `core/format.py:73`; ADR 0010 | absent | no recorded reason; found late in the Task 11 coverage sweep — no earlier surveyor read `core/models.py`/`core/format.py`, so ts's absence was never evaluated against this feature | D | Does rs's table renderer want an opt-in hook for OSC-8 hyperlinks + relative timestamps? Needs a trait-based hook, a link-escaping helper, maybe a relative-time crate | yes |

## Analyst notes

### workspace-static
- R01 item-level `why`: only F011/F018/F121 have an inferable structural reason (they follow from F001's absence in ts); F001/F002/F013 have genuinely no recorded reason in either the area file or `TS_PORT_DECISIONS.md` — marked `inferred` throughout, not `D-0nn`.
- R01/R02 both hinge on the same upstream fact (F001: does rs adopt ports/adapters at all); R02's whole tool-enforcement bundle (F003-F010, F116) is only "needed" if R01 says yes. Flagging this dependency since neither item's prompt states it explicitly.
- R02 item-level class C covers F003-F010 and F116 as one bucket (all py-only import-linter/tach/ruff-TID251/ty tooling with no ts counterpart and no recorded reason); only F020 (namespacing) and F021 (crate topology, class G) diverge enough to warrant their own rows.
- R03's class assignment (F) is uncertain: could also be read as G (Rust's `Result`/`Option` vocabulary is different enough from both py's two-step and ts's one-step that this is closer to a fresh Rust-idiom question than a "genuine disagreement to resolve once"). Chose F because both source shapes are independently viable in Rust and the prompt frames it as a binary choice between them.
- R05's F016 class (F) is likewise soft: the "why" is inferred from ecosystem-idiom framing in the area notes, not a cited decision — flagged as a candidate for owner review if a `D-0nn`-equivalent rationale turns up elsewhere.
- The dispatch's R01 fact list cites F121 under area `testing-coverage`, not `workspace-architecture` or `static-analysis`; I read `docs/port/areas/testing-coverage.md:11` directly for that one row since it falls outside the two area files named in my dispatch header.
- R30 item-level class (C) covers F105/F107/F108/F110/F112 as one bucket (all differ only because py needed two engines — `ty` CI + `pyright` IDE — and a rule-based config surface, while ts collapsed to one engine with flag-based config); F106, F111, F113 diverge enough (different underlying reason, not just "which tool") to warrant their own rows.
- F110 (rule-based `ty` config vs. flag-based `tsc` config, D-013(2)) was left inside the item-level C bucket rather than given its own row; it could also be read as class B (each language's checker exposes config differently) — flagging the ambiguity since it affects whether Rust's config surface (`[lints.rust]`/`#![warn(...)]`) is researched as a B-style "pick a tool convention" or a C-style "does this concept even apply."
- R30's own prompt already asks the right C-shaped question (`cargo check` vs. a wrapper); its D-015(2)/(3) TypeScript-7 sub-thread (tsgo, not in my item's fact list) is out of scope here.
- No ledger/area evidence found wrong; F109 (editor-extension recommendation) appears in the static-analysis area file but is not in my dispatch's R30 fact list, so it is intentionally omitted here.
### ci-a
- R09 item-level class D vs E was a close call: F031's absence traces to an org/template policy statement (`D-022(1)`, "Blacksmith explicitly not reused for a public template") rather than to Python-vs-JS mechanics, so I classified it D (one-sided, language-neutral) rather than E. If the owner reads Blacksmith-avoidance as itself a Rust-repo-specific call, E would also be defensible.
- R11 and R12 (F038/F040, F041) have no `D-0nn` recorded reason in either the area file or `TS_PORT_DECISIONS.md` for their absence in ts; I classified both E (language-specific, driven by py's larger/more fragmented Python-tool count vs ts's consolidated fast toolchain) based on inference from the tool counts, not a cited rationale — flagging in case the research prompts assumed a different class.
- R13's and R14's current prompt files were not opened (out of scope per the "read this order" instructions once the area file + COMMONALITY + decision log gave enough to classify); if either prompt's `## Questions` section asks about F042/F049 or F043/F172 as a single undifferentiated question, it should be split per the sub-row classes here (F: strategy choice vs A: threshold value, and D: redundancy question vs A: threshold value, respectively).
- F058's `harmonize: partly` reflects that it is downstream of a decision owned by a different topic (release-versioning's auth mechanism); I did not open that topic's evidence.
- No ledger/area evidence was found to be wrong; F026, F041, F038/F040 area rows simply have empty `ts-decisions` cells, which I preserved as "no recorded reason" rather than inventing a `D-0nn`.
### ci-b
- R15/R16/R26 all assigned class C on the same reasoning: the brief's own C example ("npm packing guard, wheel build, `typescript.tsdk` pin") and its listed resolution ("decide whether an equivalent mechanic exists for Rust at all") match these three better than D or E — each item's prompt HIGH questions are literally "does a Rust-native tool/extractor/mechanic exist to do this." I considered D for R15 (nothing *inherently* language-specific about a template-rebrand self-check) and E for R16 (only py "needed" the custom config), but C's resolution language fits the actual open question more precisely in both cases.
- R19: the area file's `notes` column states the grouping/ecosystem split is "one Dependabot-config-shape decision" bundling F053+F054; I kept that as a single item row per the output contract's "most items need no sub-rows" rule, since both facts land in the same class (A).
- R21: no `D-022(k)` sub-item explicitly names the GITHUB_TOKEN-vs-PAT trade-off; the "why" cited here is the area file's free-text `notes` cell, not a decision-log entry — flagged as the closest available reasoning rather than a `D-0nn` citation.
- R17/R18's prompt files ask deep questions about specific tool/version/cost trade-offs (TruffleHog alternatives, action version and secrets/API-key requirements) that fit class D's "little or no research" framing loosely — the *adopt-or-drop* call is D-shaped and needs no research, but *which exact tool/version/secret* to use if adopted is a legitimate residual question the current prompts correctly carry.
- All eight items' `ts-decisions` evidence came from the area file's `ts-decisions` column plus the cited `D-0nn` entries in `ts-launch-blueprint`'s `TS_PORT_DECISIONS.md`; no ledger/area evidence was found to be wrong, only thin (R15, R16, R17, R18 have no numbered decision, only inferred silence).
### release-packaging
- R22: borderline B vs C. Chose B (same need — expose own version — each side reaches for a language-bound mechanism) because the prompt's own questions (does `env!` "fully substitute", is there a problem it can't solve) frame this as a tool/mechanism pick, not "does the pattern exist for Rust at all" (C's frame — it clearly does, via `env!`).
- R23: the area file's own notes already partially answer the class-C question ("Cargo.lock does encode workspace-member versions, like uv.lock"), so the open part is narrower than a full "does an equivalent mechanic exist" search — closer to confirming a jsonpath-vs-companion-step implementation detail. Flagging in case the controller wants to downgrade this item's research depth.
- R24: `ts-decisions` D-021(3) explains ts's own reason for its list (inherited from `cog.toml`), not a py-vs-ts disagreement — the "why" cell reflects that; the prompt's HIGH questions correctly move past this to a fresh audience-fit call, no mismatch.
- R25: borderline D vs F. Chose D ("one-sided, language-neutral" — bump-minor-pre-major is a release-please config flag usable under any `release-type`) over F ("genuine design disagreement") because the ledger's own notes attribute the difference to each project's actual version history at port time, not a considered py-vs-ts tradeoff debate. The prompt's LOW question ("does the library-crate nature push toward pre-1.0 more strongly than it pushed for ts") is the one place the prompt drifts toward F-style "genuine disagreement" framing — worth the controller's eye if it wants a cleaner class-to-question match.
- No ledger/area evidence found wrong; F065's "notes" cell for `ts-decisions` shows `—` (no D-0nn), confirmed by re-reading the row.
- R68: mixed class. The literal F214 fact (sdist+wheel vs one tarball) is closer to C and is already effectively settled — `cargo publish` produces one `.crate` file, so Rust's shape matches ts's, no research needed on that narrow point. I assigned the item-level row class **G** instead, because the ledger's own note and the prompt's actual questions (`cargo-dist`, `cargo-binstall`, cross-compiled GitHub Release binaries) are about a capability neither py nor ts has any precedent for — that is the substantive open question this item exists to answer, not the settled one-crate-file fact.
- R50: item-level class is A (the headline question — CI-wire the smoke test like py, or stay local like ts — is a cadence pick), `harmonize: partly` because the F222 sub-row is class B (a genuine Rust tool pick for the ephemeral-install mechanism itself) and needs its own answer regardless of the cadence decision.
- No ledger/area evidence found wrong in this area file; the "public library export surface declaration" and "runtime/build toolchain version pin file" rows in `packaging-distribution.md` have no `F###` id and are not among this dispatch's assigned facts, so they were read for context only and excluded from the table.
### lint-docs
- F091 (R27): the ledger's `origin` column reads "different" even though the py cell is empty (`—`); I treated this as one-sided (ts added a formatter option py's tool structurally lacks) and classed it `D`, not `A`, since there is no py value to diverge from.
- F103 (R27): classed `A` for why py and ts differ (both are real values on a shared mechanism — floating range vs. exact pin), but flagged `harmonize: partly` because the mechanism itself (Cargo-dependency version range) may not exist for rustfmt, which ships as a rustup component rather than a crate — the Rust answer could be "not applicable" rather than a picked value.
- F097 (R29): the area file's own notes explicitly verify that ts's taplo omission is a consequence of zero `.toml` files surviving the port (a release-tooling/cog.toml decision), not a formatter capability gap — used this to override a naive "language-bound tool" (`B`/`C`) read in favor of `D`.
- F196 (R28) and F091 (R27) are the two rows where I inferred a class beyond what a `D-0nn` explicitly states; both cite the area-file notes text as the basis, not invention.
- Current prompt questions for all five items already target the class I assigned (R27/R28 ask for crate/tool picks matching `B`; R81 asks an owner-shaped org-vs-ecosystem question matching `F`; R82 asks for a tool pick matching `B`) — no item's prompt needs redirecting.
### testing
- F129 (test file organization): considered class C (Rust's `tests/*.rs`-as-separate-binary discovery could make flat-vs-subdir non-arbitrary for Rust), but the *recorded* reason (D-019(2)) is org-precedent-based, not a language mechanic, so I classified it F. Flagging the C candidate since the actual Rust answer may end up mechanic-constrained regardless of the recorded ts rationale.
- F130 and F124/F125: the area file's `ts-decisions` column is empty (`—`) for these three rows — no `D-0nn` explains the omission from Vitest, so all three `why` cells are `inferred:`, not sourced from a decision log entry.
- F131: classified E rather than B because the underlying "need" (an opt-in parallel-execution flag) exists only because pytest itself defaults to serial execution; neither Vitest nor `cargo test` need an equivalent flag since both parallelize by default — this is a case where "no action" is the likely Rust answer, but the R32 prompt still asks it as an open HIGH question.
- R32's prompt (`test-harness-and-execution.prompt.md`) frames nearly the whole bundle as one research decision; per-fact classification here shows only F117 (runner pick), F131 (parallelism, likely "none needed"), and F132 (Rust-only doc-test policy) are genuine open research; F118/F124/F125/F128/F129/F173 are convention/adoption calls with little crate research required, and F130 is a language-mechanic gap. The prompt's own HIGH/MEDIUM/LOW tiering already roughly matches this split.
- F136/F137 (coverage threshold location and CI enforcement): both classified F (genuine, still-unresolved design disagreement) since py and ts themselves already differ with no evidence either was trying to converge — this is presented as a live owner call, not a research-answerable question.
- R34/F127: is a strict area-note-derived read; I did not independently verify whether Vitest's `sequence.shuffle` (cited for F124) is enabled anywhere in ts's config beyond what the area file states — flagging as evidence taken at face value from the ledger's own text, not independently re-verified against ts source.
### git-hooks
- F152/D-020(2) evidence conflict: the area file's own note says "py never overrides `header-max-length` in `commitlint.config.mjs` (inherits config-conventional's 100-char default), unlike the 50/72 contract D-020(2) attributes to the source" (`docs/port/areas/git-hooks-commit-hygiene.md:17`). D-020(2)'s decision text (`docs/port/TS_PORT_DECISIONS.md` @ ts `cb1cbcb`, entry `D-020: Phase 4 research — Git hooks & commit linting`, item 2) reads "reconcile ... overrides header-max-length 50, body-max-line-length 72" as if both were carried from py; only body-max-line-length (F153) is actually a py override — header-max-length (F152) is a ts-only addition. I did not fix this; flagging per the brief's instruction to cite, not fix.
- F149 class choice: I assigned B ("Rust needs its own tool pick; crate/tool search") even though B's stated definition ("each with a language-specific tool") doesn't literally hold — py and ts use the *same* tool (commitlint), and neither has a native-to-its-language alternative. G was ruled out because G requires "no precedent in either repo," and precedent (commitlint) exists in both. B's typical-resolution description is the closest fit to the actual open question (does Rust find/build a native crate, or also reach outside its ecosystem), so I used it with this caveat recorded here rather than inventing a class.
- F167 class choice: torn between C (assigned — the answer depends on the specific formatter Rust picks and that tool's feature coverage) and F (both "separate validity linter" and "fold into formatter" are viable, language-neutral designs). Chose C because the prompt's own HIGH question ties the decision directly to rustfmt's existing capability, not to an independent design preference.
- Prompt/class mismatches: R39's prompt runs two HIGH questions comparing gitleaks against a hypothetical "Rust-native alternative," which reads as tool-search framing (class B territory) even though I classified the item D (one-sided, language-neutral, adopt-or-drop) — gitleaks itself needs no substitute since it's already language-agnostic; the real open question is adopt-or-drop, not a crate search. Likewise R40's MEDIUM questions on F168/F169 ask for "Rust-native equivalents worth comparing," which is consistent with a tool search once "adopt" is chosen, but the prior adopt-or-drop question (which I classified D) isn't posed by the prompt at all — the prompt presumes adoption and skips straight to tool choice.
- F156's absence in ts is explicitly recorded in the area note ("Selected in CI by PR-author login, not applicable without ts's equivalent CI job," `docs/port/areas/git-hooks-commit-hygiene.md:21`) — not marked `inferred:` since a reason is on record, even though no `D-0nn` id backs it.
### dev-experience
- R42 item-level class (E) is my inference, not recorded: no `D-0nn` explains ts's *omission of a provisioner entirely* (only D-014(6)/(7) explain the narrower taplo/yamlfmt installer-recipe removal, which I split into the F183 sub-row as C). I chose E over D because the item's own prompt (`research/topics/42-.../prompts/dev-toolchain-provisioning.prompt.md` HIGH Q1/Q2) asks a need-based question ("does Rust's narrower non-cargo tool surface make a provisioner unnecessary"), which is E-shaped, not a plain adopt/drop D question. Flagging the alternate read in case the controller weighs it differently.
- F183's ledger fact (`docs/port/areas/dev-experience-repo-hygiene.md:14`) bundles taplo/yamlfmt (explained by D-014(6)/(7), class C) together with gitleaks/actionlint (unexplained by any `D-0nn`, and explicitly out of scope for R42 per its own "Out of scope" section — R39/R40 own those tools). The area file doesn't separate them; I only classified the in-scope taplo/yamlfmt mechanic.
- R44's harmonize is `partly`: the "have a devcontainer at all" choice is language-neutral (D, harmonize yes-or-no-for-all-three), but a base image and in-container config, if adopted, would be Rust-specific with no precedent in either source repo (G-shaped). The item's own prompt already researches both layers, so no prompt/class mismatch — just noting the split for the harmonize column.
- No ledger/area evidence found wrong; all `path:line` citations above are copied directly from `docs/port/areas/dev-experience-repo-hygiene.md`.
- All 6 items' current prompt questions are consistent with the classes assigned above (R42's mixed E/C, R43's mixed F/D, R44's partly, R45/R46/R47's D/D/A) — none needed a class correction against its prompt.
### config
- R54: classed D on the inference that ts's own D-017(1) target precedence already includes the project/system tiers py has; the decision log records the *intent* but not why the shipped code stopped at the user tier. If that gap turns out to be a deliberate ts scope cut rather than a deferral, reclassify toward F.
- R55/F235: ts's throw-on-missing-path behavior cites `click.Path(exists=True)` parity in its own code comment, but the area file's notes explicitly say the pinned py commit's `--config` option does not set `exists=True`. This looks like ts's rationale may rest on a mismatched assumption about py — flagging so R55's research doesn't treat ts's stated reason as authoritative without re-checking.
- R56: classed F239/F240 as F (owner security-policy call) rather than B/C since nothing about serde/clap forces either precedence — this is a values decision (py's ADR-0002 vs cli-standards' generic chain), not a mechanics one.
- R58/R59 prompts are framed almost entirely as crate-selection research (`tracing` vs `env_logger`, rotation-crate survey), consistent with class B — but the actual divergence is a scope/richness choice (F) that logically precedes the crate question. Recommend the research surface the scope decision explicitly before defaulting into a crate comparison.
- F254 (explicit `--log-level` override) cites D-018(3), which is really about the verbosity-ladder mapping, not a direct rationale for omitting an absolute-level flag; if cli-standards' ladder-only model is meant to be normative org-wide (not just a ts choice), this one fact could converge toward D/harmonize=yes rather than riding along inside R58's broader F classification — left bundled since it's one fact among seven in the same item.
### cli
- R62/R63: D-016(7) ("pyperclip replacement") and D-016(5) ("rich.Progress replacement") phrase these ts decisions as replacing a Python-specific tool, implying py precedent for clipboard-copy and progress-spinner — but the cli-framework-ux area file cites no py `path:line` for either (both marked `ts-only`). Treated the area file as ground truth per the brief; flagging the mismatch rather than resolving it.
- R64 (pager) and R66/F291 (3rd output format) are classified D and F respectively, but both are defensible as class F (genuine design disagreement/owner call) instead — neither area evidence nor the decision log states *why* ts omitted paging or picked csv over markdown, so the D-vs-F line rests on my inference that these are scope gaps rather than deliberate rejections.
- R67 is a genuine 4-class bundle (A: exit-code taxonomy/signals: F297/F298; C: language-bound mechanics F295/F300; D: one-sided F299/F302; F: design disagreement F296) — no single class fits the item-level summary row, so I used F there and pushed precision to the seven sub-rows.
- F296 (`hint` field) and F300 (EPIPE) have no `D-0nn` citation anywhere in the area file or `TS_PORT_DECISIONS.md`; both `why` cells are fully `inferred:` with no recorded reason to fall back on.
- R60/R66/R67's current prompt `## Questions` already frame these correctly as crate/tool-search (R60), naming/format-choice (R66), and taxonomy/contract (R67) questions consistent with my class assignments — no prompt-vs-class mismatch found worth flagging.
### web-a
- **Controller ruling (2026-09-02):** all 8 items reclassed `F` → `B` for consistency with the other 7 web-service items (R76–R80, R83, R84, classed `B` by their analyst). Reason: the scope-level cause — ts dropped the whole web tier, unrecorded, language-neutral — is one fact (F303), and spec §4 D7 (target shape CLI + library + web service) has already re-adopted py's shape for rs, so no item here asks the owner to "decide once for the org" (the effect `F` would signal). What remains per item is a language-bound tool or crate pick with py as the only precedent, which is `B`'s effect: Rust decides alone. The one genuinely org-level question — should ts gain a web tier too — is carried once, as `harmonize: partly` on R69. The analyst's original reasoning for `F` is preserved below.
- Class assignment (all 8 items) is deliberately `F`, not `B` or `E`, and I was genuinely unsure between the three — recorded here per the brief's instruction to flag uncertainty:
  - `B` ("same pattern, language-bound tool") is the class the prompts themselves are structured for (crate/tool search, same resolution language as B's typical resolution) — but B's definition requires *both* repos to do the thing with different tools, and ts has literally nothing (no web/HTTP-server code anywhere in the ts repo, per `docs/port/areas/web-service.md`'s own Files-read note).
  - `E` ("one-sided, language-specific") doesn't fit either: nothing about a web-service tier is intrinsically Python-specific — TypeScript has full ecosystem capability (Express/Fastify/Zod, OpenAPI generators, idempotency/rate-limit middleware all exist on npm) — so the absence isn't a language limitation.
  - I chose `F` ("genuine design disagreement... decide once for the org and apply to all three, OR accept per-repo choice — owner call") because that's what actually happened: ts's own README scopes the project as "CLI + library" only (a deliberate, if unreasoned, scope choice — no `TS_PORT_DECISIONS.md` entry addresses it), and the owner has *already* exercised the "accept per-repo choice" branch of F's resolution at `docs/port/COMMONALITY.md` row F017 (area `workspace-architecture`, verdict `ADOPT`, no research item): rs gets an optional web Cargo feature mirroring py, with no matching retrofit proposed for ts. `harmonize: no` reflects that this asymmetry is already the accepted state, not an open harmonization question — the 8 items here only research *how* Rust fills the slot F017 already opened.
- No `D-0nn` entry in `ts-launch-blueprint`'s `docs/port/TS_PORT_DECISIONS.md` (checked all 38 entries, `D-001`–`D-038`) addresses web-service scope at all — every "why" cell above is `inferred:` from the ts README's own project description plus the area file's own Files-read note, not from a recorded decision. Flagging so the research items don't cite a `D-0nn` that doesn't exist.
- All facts within each bundle (e.g. R71's F307/F312/F332/F333 mixing a tool-integration fact with two convention-shaped facts) share the same root cause (ts absent the whole web tier), so I kept every item as a single row per the brief's "most items need no sub-rows" guidance rather than manufacturing sub-row splits that wouldn't change the class.
- The item-level prompts (R69, R51 read in full; R70–R75 `## Questions` sections read) already correctly scope themselves as tool/pattern research contingent on F017's existence decision and do not re-litigate "should rs have a web service" — no prompt/class mismatch found.
### web-b
- All seven items share one root cause, not seven independent ones: F303 ("web service exists at all") is `py-only`, and the area file's own "Files read" list states plainly that `TS_PORT_DECISIONS.md` "records no decision entry addresses web-service scope; the module is absent and unindexed in TS_PORT_INDEX.md" — i.e. ts simply never built any web server, so nothing under this area has a ts side to diverge from. Confirmed independently via `git -C /Users/stevemorin/c/ts-launch-blueprint show cb1cbcb:docs/port/TS_PORT_DECISIONS.md`, which contains no entry mentioning web/HTTP-server scope among its 38 D-0nn entries.
- Class choice (B vs D) was the one judgment call, made the same way for all seven: table class D ("one-sided, language-neutral … resolution: adopt in all three or drop in all three") technically fits the raw fact (ts lacks it, nothing about the concept is TS-specific), but every item's prompt already treats "adopt" as settled — Rust's target shape (CLI + library + web service, spec §4 D7) is fixed and out of scope for these items, and each prompt's Item kind is `crate` (R80: `bundle`) asking only which Rust crate/pattern fills the inherited-from-py slot. That framing matches B ("same pattern, language-bound tool") once ts's whole-module absence is read as "no tool to compare," not as evidence the pattern itself is non-language-neutral. I chose B for all seven on this basis; a reader could reasonably argue D with a note that adoption is pre-settled.
- No sub-rows were added for the R80 bundle (F328+F329): F328 (version reporting) leans toward a plain crate pick and F329 (shared CLI/web diagnostics) leans toward an architecture convention rather than a crate pick, but both trace to the same F303 root cause and the same B classification, so splitting would not surface a different class — kept as one item row per the brief's "most items need no sub-rows" guidance.
- Every item's prompt already matches its assigned class (all ask "which crate/tool" against a py-only pattern with no ts comparator) — no prompt/class mismatches found.
- No ledger or area-file evidence was found to be wrong or missing for these seven items; the `docs/port/COMMONALITY.md` split-provenance notes for F334/F335 (Task 10 reconciliation, 9d review Minor 3) were verified present at `docs/port/COMMONALITY.md:338` and `:339`.
